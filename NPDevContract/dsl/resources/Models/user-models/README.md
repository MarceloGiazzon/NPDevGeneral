# User Models Category

This directory is reserved for user-authored and workspace-local models.

Category intent:

- flexible and non-platform-owned
- safe place for experimentation
- no assumption of global naming or version stability

Scripts may target this root directly, or a specific user model subdirectory when one exists.
