# Tenant Samples

This folder contains model packs used to explore tenant-shaped generated applications.

Current sample:

- `restaurant-saas-multitenant`: Pizza House, Sushi Bar, and Vegan Bistro share one generated restaurant SaaS app, with tenant ownership modeled through a `Tenant` concept and required `tenantId` references.

The local catalog is `catalog.json`.
