# Expected Endpoints

## Expected REST surfaces

- `GET /api/flows`
- `GET /api/admin/model/export`
- `GET /api/admin/model/metadata`
- `GET /api/audit`

## Expected tenant-oriented CRUD paths

The generated app should expose `/api/` CRUD-style routes for tenant-owned concepts and enough `GET ` evidence surfaces to inspect tenant-shaped runtime behavior.
