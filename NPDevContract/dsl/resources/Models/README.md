# NPDev Model Categories

This folder is the authoritative category anchor for NPDev model assets.

- `canonical-demo` defines the fixed platform specimen lane
- `official-samples` defines the curated learning and trial lane
- `tenant-samples` defines tenant-shaped SaaS examples
- `user-models` defines the open-ended user-authored lane

The category manifest for scripts lives in `model-categories.json`.
