# Runtime Manifest Contract

Runtime manifests are generated from the current model/config and consumed by the generated app at execution time.

Security manifests, UI metadata policy, plugin manifests, runtime overrides, and binding manifests should be generated or selected for the current app instead of carrying sample-specific hardcoded concepts.

Canonical schema paths live under schemas/runtime and schemas/kernel.
