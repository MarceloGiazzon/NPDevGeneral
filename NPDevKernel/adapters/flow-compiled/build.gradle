dependencies {
    implementation project(':kernel')
    implementation project(':dsl')
    // dsl exposes Jackson (JsonNode) on its public JsonModelParser.parse(...) API as
    // implementation, so consumers that call it need Jackson on their own compile classpath.
    implementation 'com.fasterxml.jackson.core:jackson-databind:2.17.2'

    testImplementation project(':dsl')
}
