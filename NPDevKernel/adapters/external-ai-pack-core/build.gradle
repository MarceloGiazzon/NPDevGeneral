dependencies {
    implementation project(':kernel')
    // ADR-0009 / P6: reuses the SAME secret-content-patterns.json classpath resource
    // tracing-redaction-default's SensitiveKeyPolicy loads from -- one rule source, not a second
    // independent copy for the in-app producer.
    implementation project(':adapters:tracing-redaction-default')
    implementation 'com.fasterxml.jackson.core:jackson-databind:2.17.2'
}
