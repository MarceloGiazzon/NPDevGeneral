id: RUN-5
title: >-
  expression-cel has no CEL dependency; the name misleads
type: GAP
severity: LOW
status: DONE
opened: 2026-08-12
closed: 2026-08-20
resolution: >-
  Renamed module from expression-cel to runtime-support. Updated directory
  (NPDevKernel/adapters/runtime-support), Java package (com.npdev.adapters.expression.cel →
  com.npdev.runtime.support), CelInvariantEngine moved to match, and all 598 references across
  settings.gradle, build.gradle files, CI workflows, policy JSONs, quality scripts, docs, and
  ledger items. Zero functional changes -- rename only.
source: >-
  Full-repo maturity audit 2026-08-12 (MASTER-ROADMAP.md, ROADMAP.md card R8, "also here" note). The
  expression-cel adapter module had no CEL dependency anywhere in any build file -- the name was the
  only thing wrong with it.
surface: kernel/adapters/runtime-support
files:
  - NPDevKernel/adapters/runtime-support
detail: |
  Renamed 2026-08-20. The module was always a runtime-support adapter (GeneratedCrudRuntimeSupport,
  CelInvariantEngine, IdentityRoleLookup, FileOrphanSweeper, scheduling, value coercion, etc.) with
  no actual CEL library dependency. New name `runtime-support` matches the existing Java package
  `com.npdev.runtime.support` where most classes already lived.
