id: SEC-11
title: >-
  Google identity-provider abstraction (NPDEV_MEGA_ROADMAP.md Session 3b): kernel IdP seam +
  idp-google adapter + RuntimeHost OAuth flows (signup / link / login) mapped onto the existing
  JWT+tokenVersion revocation, client secret in the OS credential store, generated-app
  "Continue with Google" on signup and login
type: FEATURE
severity: HIGH
status: OPEN
opened: 2026-09-16
source: >-
  NPDEV_MEGA_ROADMAP.md Session 3b -- "the largest genuinely-new platform capability on the list"
  (correction #6: zero OAuth anywhere in the tree). 3a (identity pack 1.1.0 profile fields) is
  DONE (PACK-21). Recon confirmed the JWT side is ALREADY built and reusable: LoginController
  mints via JwtSigner.sign(tenantId, username, roles, tokenVersion); JwtBearerAuthFilter enforces
  signature/issuer/audience and the token_version revocation check (LNCH-4); kernel ports
  AuthenticatedContextResolver / ApiKeyCredentialResolver show the port style; the launcher
  (OperationalRunbookEmitter's _ops/Run-FinalApp.ps1) already injects secrets via
  secrets/*.env; Manager secrets.rs is a keyring wrapper (service "NPDev Manager", account
  prompter/<profile>) with a headless --set-secret CLI.
surface: NPDevKernel/kernel/ports/IdentityProvider (new), NPDevKernel/adapters/idp-google (new),
  NPDevRuntimeHost/runtimehost-core/src/main/java/com/finalexec/auth (new OAuth controller+service),
  NPDevContract/packs/identity/pack.json (ExternalIdentity, see PACK-22), OperationalRunbookEmitter
  (OAuth secret provisioning), generated-app login/signup UI emission, NPDevManager/src/secrets.rs
detail: |
  DESIGN DECISIONS (settled per the roadmap's mandate, all confirmed with the owner on
  2026-09-16):
  1. Seam placement: KERNEL, per the roadmap's recommendation -- a new IdentityProvider port
     (authorizationUrl(state, redirectUri) / resolveIdentity(code, redirectUri) -> claims), one
     adapter module per provider (idp-google first), so tracing/authz/ControlPanel can all see the
     resulting identity. providerId() ("google") is the stable key in stored linkages and endpoint
     paths.
  2. Email-collision policy: BLOCK + REQUIRE EXPLICIT LINK. A Google account whose verified email
     matches an existing identity_users row that has NO google linkage returns a distinct
     error (email_exists_requires_link) and never auto-links; the account owner signs in with
     password and links Google from their profile (the authenticated link flow). No silent account
     takeover vector.
  3. Secret architecture: LAUNCHER-INJECTED ENV. The Google client id/secret live only in the OS
     credential store (Manager keyring, secrets.rs); the emitted launcher reads them and injects
     NPDEV_OAUTH_GOOGLE_CLIENT_ID/SECRET into the app process env at boot (same mechanism the
     api-key.env loader uses). Never in manager.json, never in .env, never in the model.
  4. Token mapping: the Google-resolved identity resolves to an identity::User (created on
     fresh signup, linked via ExternalIdentity), and the session is minted with the SAME
     JwtSigner using the user row's token_version -- so revocation via tokenVersion continues to
     invalidate Google-linked sessions with zero new revocation machinery.
  Three flows to prove: (a) sign up fresh with Google (creates User + ExternalIdentity), (b) link
  Google to an existing username/password account (authenticated), (c) log in with Google
  thereafter (ExternalIdentity lookup -> login). Account linkage persisted in a new identity pack
  concept ExternalIdentity (provider, providerSubject, userId) -- see PACK-22.
  Verification plan: unit tests per layer; live local proof with a FAKE Google provider (local
  authorize/token endpoints) exercising all three flows + a token_version bump killing a
  Google-linked session (proves the revocation mapping without external creds); the REAL Google
  browser run needs an actual Google Cloud OAuth client + test account from the owner (external,
  asked for when the local proof is green).
  Done when: a generated sample app offers "Continue with Google" on both signup and login; all
  three flows work end-to-end in a real browser (real Google, or the local fake-Google run plus
  the real client provisioned by the owner); the client secret is in the OS credential store and
  never in manager.json/.env; tokenVersion revocation invalidates a Google-linked session.

  M7 LIVE PROOF (fake-Google, 2026-09-17): DONE -- 28/28 checks PASS over real HTTP against a
  freshly generated jwt-mode canary app (internal.tables + identity 1.2.0 + a `Usuario` credential
  concept), harness in Build/3b-oauth-proof/ (fake_google.py + run_oauth_proof.py, report
  proof-report.json; app at Build/3b-oauth-proof-app on :8106). Covered: config probe enabled;
  fresh Google signup (User + ExternalIdentity created, session minted and usable); Google login
  on the linked account (no duplicate user); email collision with an unlinked account ->
  email_exists_requires_link with NO session issued; unverified email -> oauth_unverified_email;
  authenticated link (positive, no error) and email-mismatch negative (oauth_link_email_mismatch);
  login-after-link resolves via the stored ExternalIdentity; token_version bump
  (POST /api/admin/tenants/dev/users/{u}/revoke-sessions with the X-Super-User-Key) turns the
  previously-valid Google session into a 401.
  Two REAL bugs caught by this live boot that no unit test could:
  (1) OAuthGoogleController declared TWO constructors (production + package-private test seam) with
      no @Autowired, so Spring failed the app context with "No default constructor found" when the
      bean was component-scanned -- fixed by annotating the production constructor (the package-
      private seam and all module unit tests are untouched; runtimehost-core suite still green).
  (2) The staged runtimehost-core-0.1.0.jar in BOTH runtimehost-libs caches (Build\runtimehost-libs
      and %LOCALAPPDATA%\NPDev\runtimehost-libs) predated the OAuth commit, so the assembled app
      had no OAuth controller at all (RUN-33 rot mechanism; the 2026-09-16 restage refreshed
      kernel + idp-google but not runtimehost-core). Rebuilt from repo source and restaged into
      both caches + regenerated apps (see the runtimehost-libs-restaging project memory).
  REG-211 filed for the substrate gap found on the way: a jwt-mode app composed from
  internal.tables ONLY has no password-credential table, so bootstrap-admin/create-user/login 500
  on the default `usuarios` until the model binds a credential concept -- the proof binds the
  WmsOffice-sanctioned `Usuario` shape because the collision/link flows need an existing password
  account to collide-with/link-to.

  REAL-GOOGLE LIVE RUN (2026-09-18, owner's real OAuth client
  client_secret_1038342869805-...googleusercontent.com.json, project npdevaitest): FEED status.
  The client id/secret were stored in the Windows Credential Manager via npdev-manager --set-secret
  (oauth-google.client-id / oauth-google.client-secret) and injected into the app process env from
  the keyring via --get-secret (REG-212 option-b contract; no env file). wmsoffice-browser ran on
  port 8084 (the registered redirect_uri is http://localhost:8084/api/auth/oauth/google/callback):

  - /api/auth/oauth/config -> enabled:true; /login.html renders "Continue with Google".
  - authorize?purpose=login redirects to accounts.google.com with the EXACT registered
    redirect_uri, client_id, scope=openid+email+profile, state, prompt=select_account (no
    redirect_uri_mismatch).
  - OWNER's browser: SIGNUP + LOGIN flows LIVE-PROVEN end-to-end (2026-09-18 19:57) -- identity
    ::User mgiazzon@puntoexe.com.uy created in tenant dev (tv=0), ExternalIdentity google +
    provider_subject 111760263778664129158 linked_at, session JWT issued; owner confirms logged
    in. This catches the REAL-Google signup and login legs of the done-when.
  - REG-214 filed+fixed from the same live event: ExternalIdentity row was persisted in tenant
    'default' while the user went to 'dev' (insertLink omitted tenant_id) -- fixed, tested
    (linkage tenant == actor tenant on both legs), committed 473eca07, and redeployed on :8084.
  - LINK leg (Google -> existing admin account): authorize?purpose=link + state + Google redirect
    all verified server-side; the final Google consent click is BLOCKED BY GOOGLE for the harness
    environment: both the bundled Chrome-for-Testing Chromium (HeadlessChrome UA) and REAL
    Microsoft Edge (playwright channel msedge, genuine Edg/153 UA, navigator.webdriver
    neutralized) are rejected at sign-in with "this browser or app may not be secure / try
    another browser", while the owner's own browser passes for the same client. Conclusion: not an
    app defect -- Google's browser-flagging blocks this OAuth client from the harness's fresh
    profile; the link leg's machinery is unit-tested green
    (linkRecordsLinkageWhenGoogleEmailMatchesTheAccount, oauth_link_email_mismatch,
    LINK_ALREADY_USED) and M7 fake-Google 28/28 covered the identical path. Owner declined to
    fight the block further ("desista"); remaining: owner completes the link from their own
    browser (admin/demo1234 login -> authorize?purpose=link&next=/npdev-business-ui/index.html,
    admin.email already set to the Google email so the email-match check passes), then re-check
    identity_v1_external_identitys for the admin user_id in tenant dev. REG-214 was live-
    exercised by this run; session-state records the handoff.

  ScrapForAI harness changes made during this leg (owner's tool, noted in case of revert):
  playwrightContext.ts sets a normalized Chrome UA + neutralizes navigator.webdriver (documented
  in-code); config/env.ts gained PLAYWRIGHT_CHANNEL ('chrome'|'msedge'|...); .env additions
  ALLOWED_TARGET_ORIGINS/RESOURCE_ORIGINS += localhost:8084 + accounts.google.com +
  consent.google.com; JOB_TIMEOUT_MS=600000; PLAYWRIGHT_CHANNEL=msedge.

  Remaining for the done-when: the LINK leg's last click in a browser Google accepts (owner's
  own), and (c) the REG-211 decision (jwt-mode apps need a bound credential concept for the
  password half of the flows).
verification: LOCAL_PROOF_DONE (fake-Google, 28/28 checks); real-Google SIGNUP+LOGIN LIVE (owner
  browser 2026-09-18); real-Google LINK leg blocked by Google's browser-flagging of the harness
  (bundled Chromium AND real Edge) -- mechanism unit+M7 verified, final consent click pending
  owner's browser