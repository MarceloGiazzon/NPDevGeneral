id: STOR-13
title: >-
  EIGHT SqlDialect methods have no production caller -- exercised only by their own tests, which is
  the exact state STOR-4, STOR-5 and STOR-6 were each found in (filed as nine; see
  round3_correction -- six of the original nine were false alarms, `supports` most importantly of
  all, and three of the eight are asked by the conformance vectors)
type: BUG
severity: MEDIUM
status: DONE
opened: 2026-08-09
closed: 2026-08-10
verification: UNIT_TESTED
surface: kernel/storage-dialect
source: >-
  storage/closeout/CLOSEOUT_PLAN.md section 7 ("what test would have caught all three?"). Found by
  the check that section asked for -- scripts/quality/check-dialect-methods-are-asked.py -- on its
  first run, against the tree at the moment STOR-6 closed.
detail: >-
  The plan's closing question was: STOR-4 (drivers), STOR-5 (guarded DDL) and STOR-6
  (`quoteIdentifier`, zero calls in the generator) are the same defect three times, so what check
  catches the family rather than the instances? The answer -- a check that the thing a user runs
  ASKS the dialect, not merely that the dialect answers correctly -- was built, and it immediately
  found nine more.

  Each of these is declared on `SqlDialect`, implemented by all four dialects, and called from
  nowhere in `NPDevKernel/*/src/main`, `NPDevGenerator/generator/src/main`,
  `NPDevRuntimeHost/src/main` or `NPDevContract/dsl/src/main`. The test-caller counts below are
  dialect-receiver calls found under `src/test` -- proven correct, wired to nothing:

      supports                       12 test callers, 0 production
      autoIncrementColumn             9 test callers, 0 production
      rowLimit                        7 test callers, 0 production
      returning                       5 test callers, 0 production
      listColumnsSql                  4 test callers, 0 production
      limitOnly                       4 test callers, 0 production
      listTablesSql                   3 test callers, 0 production
      listIndexesSql                  3 test callers, 0 production
      cast                            0 test callers, 0 production
      timestampColumnType             0 test callers, 0 production
      requiresOrderByForPagination    0 test callers, 0 production

  `supports` is the one worth reading twice. CLAUDE.md instructs the reader to
  "ask `SqlDialects.active().supports(...)` rather than assuming a rollback" -- STOR-2's whole
  remedy -- and no production code does. The capability set is consulted through
  `capabilities()` by `StorageCapabilityGate` at GENERATION time; nothing asks at RUNTIME, which is
  where the DDL-in-transaction question actually gets decided.

  Not necessarily nine bugs. Some of these may be genuinely premature -- an answer prepared before
  its consumer exists is not wrong, it is early. What was wrong is that nothing distinguished
  "prepared early" from "wired and forgotten", and that is the distinction the three closed items
  each turned out to need. They are now enumerated in that checker's INTERNAL_ONLY allowlist with
  this item's id, so the list is a visible backlog rather than an invisible one, and any NEW dialect
  method must be wired or explicitly recorded here.
remediation: >-
  Per method, decide and record: wire the production caller, or delete the method, or state in the
  allowlist why the answer exists before its consumer. `supports` should be first -- STOR-2's
  documented remedy currently has no runtime implementation.

round3_correction: >-
  2026-08-09, round3 Part 2.2. The plan asked for nine independent decisions and warned they might be
  three groups. Measured: they are SIX FALSE ALARMS plus eight genuinely-unasked methods in three
  groups -- and the most important correction is that the item's headline claim about `supports` was
  wrong.

  `supports` IS asked at runtime. `PartialApplicationTruth.afterRollback()` calls it, and
  `ConversionHookRunner` (line 405) and `SchemaHistoryStore` (line 317) call THAT on the real
  failure path. So a user on MySQL or H2 IS told their schema changes were already committed --
  STOR-2's documented remedy runs. This item recorded the opposite ("nothing asks at runtime"),
  because check-dialect-methods-are-asked.py excludes the dialect package from its caller search and
  PartialApplicationTruth lives there. The exclusion is right for its purpose; repeating its output
  as a statement about the system was not.

  Five more were the same artifact, each reached one hop away through the interface's own public
  API: limitOnly (<- limited()), rowLimit (<- rowLimited()), quoteIdentifier and
  foldsUnquotedIdentifiersToLowerCase (<- identifier()), requireOrderedForPagination
  (<- paginated()). The checker now follows exactly one hop -- a same-package helper that is itself
  used from outside -- so this class of false alarm cannot recur. One hop only: an arbitrary-depth
  walk would eventually declare everything asked.

  The eight that remain are not one backlog:
    PREPARED EARLY (4)  autoIncrementColumn, timestampColumnType, cast, returning -- an answer
                        written before its question exists is early, not wrong.
    REDUNDANT (1)       requiresOrderByForPagination -- requireOrderedForPagination() demands
                        ORDER BY of every engine regardless, so no caller can act on this. It is the
                        strongest delete candidate of the eight: delete it, or make the enforcement
                        conditional on it, but not both as they are.
    INTROSPECTION (3)   listTablesSql, listColumnsSql, listIndexesSql -- asked by the conformance
                        vectors against real engines, which is a real consumer.

resolution: >-
  2026-08-10. Closed as the round3 correction predicted: ONE deletion and SEVEN re-classifications.
  Seven of the eight needed a true reason recorded, not code -- and that is the honest outcome, not a
  shortfall. The item's value was never the eight methods; it was that nothing had distinguished
  "prepared early" from "wired and forgotten", and that distinction is now written where the checker
  reads it rather than in an item nobody opens.

  DELETED (1). `requiresOrderByForPagination` is gone from `SqlDialect` and from all four
  implementations (H2 false, MySQL false, Postgres false, SQL Server true), plus the anonymous
  dialect stub in `StorageCapabilityGateTest`. Deleted rather than made load-bearing, because the
  alternative -- making `requireOrderedForPagination()` conditional on it -- would reverse conformance
  vector P3's pinned decision. P3 refuses an unordered paginated query on EVERY engine on purpose:
  injecting an order on the one engine that needs it hides the difference from the model and STILL
  returns overlapping pages, trading a loud failure for a silent wrong answer. So the flag could only
  ever have been decoration, and decoration that reads like a gate is worse than nothing -- the next
  reader budgets for a conditional that does not exist. The engine-level FACT it carried (SQL Server
  alone rejects the unordered query itself) is not lost: it is stated in
  `SqlDialect.requireOrderedForPagination`'s javadoc and in `SqlServerDialect`'s class javadoc, which
  is where a reader looks for it.

  RE-FILED AS DELIBERATE (4). autoIncrementColumn, timestampColumnType, cast, returning. Each
  allowlist entry now says WHY the answer exists before its question -- "ids are UUIDs", "types go
  through portableColumnType()", "no site has needed the dialect to arbitrate CAST",
  "INSERT reads generated keys through JDBC getGeneratedKeys" -- rather than the bare "no caller"
  that reads as dead weight to the next person. They stay because the engines genuinely differ and
  re-deriving the differences later is the expensive half.

  RE-FILED AS COVERED (3). listTablesSql, listColumnsSql, listIndexesSql. Measured, not assumed:
  `DialectConformanceTierATest` asserts their shape on all four dialects (lines 632-658) and
  `DialectConformanceTierBTest` EXECUTES `listColumnsSql` against real engines (line 375). That is a
  real consumer which this checker's production-only search roots structurally cannot see -- which is
  precisely why it has to be stated in the allowlist or it looks identical to "nobody asks".

  WHAT KEEPS IT HONEST. `check-dialect-methods-are-asked.py` fails on an allowlist entry for a method
  the interface no longer declares, so the deletion could not be half-done. Verified: the checker
  reports 41 declared methods, 930 production files searched, OK; `:kernel:compileJava`,
  `:kernel:compileTestJava` and `:generator:compileTestJava` all compile; `DialectConformanceTierATest`
  (all four dialects) passes.
