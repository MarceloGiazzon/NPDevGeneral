# target/ itself is already redirected out of the repo entirely (see .cargo/config.toml, M0) --
# this is a belt-and-braces entry in case that config is ever missing.
/target/

# Auto-regenerated by `cargo build`/`cargo tauri build` from the installed tauri crate version on
# every build (capability/ACL JSON schemas for editor tooling) -- build output, not source, same
# category as target/ under this repo's own "never write build artifacts inside the repo" policy.
/gen/
