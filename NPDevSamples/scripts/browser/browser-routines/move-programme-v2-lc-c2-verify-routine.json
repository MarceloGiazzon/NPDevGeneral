{
  "scenarioName": "programme-v2-lc-c2-verify-hot-swapped-label",
  "targetPath": "/login.html",
  "options": { "headless": true, "screenshots": "always", "collectDomOnFailure": true },
  "steps": [
    { "action": "goto", "url": "$targetUrl", "label": "Open the real login page" },
    { "action": "waitForSelector", "selector": "#username", "state": "visible", "label": "Login form rendered" },
    { "action": "fill", "selector": "#base", "value": "http://127.0.0.1:8100", "label": "Same-origin base (127.0.0.1, never localhost)" },
    { "action": "fill", "selector": "#username", "value": "admin" },
    { "action": "fill", "selector": "#password", "value": "admin123" },
    { "action": "fill", "selector": "#tenant", "value": "trial" },
    { "action": "click", "selector": "#loginBtn", "label": "Submit real login" },
    { "action": "waitForSelector", "selector": "#tokenBox", "state": "visible", "label": "Login succeeded" },

    { "action": "goto", "url": "http://127.0.0.1:8100/npdev-business-ui/", "label": "Open the generated business UI shell" },
    { "action": "waitForTimeout", "timeoutMs": 3000, "label": "Let the shell + panel manifest load" },
    {
      "action": "evaluate",
      "name": "open_recebimento_crud",
      "script": "var links = Array.from(document.querySelectorAll('#sideNav a')); var target = links.find(function(a){ return a.getAttribute('href') === '#concept-Recebimento'; }); if (!target) return 'NO_LINK'; target.click(); return 'clicked';",
      "label": "Open the raw CRUD concept list for Recebimento"
    },
    { "action": "waitForTimeout", "timeoutMs": 2000, "label": "Let the panel render" },
    { "action": "screenshot", "name": "shot_lc_c2_recebimento_after_hotswap", "label": "Recebimento panel after the metadata-only hot swap" },
    {
      "action": "evaluate",
      "name": "panel_heading_text",
      "script": "var sec = document.querySelector('#concept-Recebimento'); if (!sec) return 'NO_PANEL'; var h = sec.querySelector('h2,h3,h1'); return JSON.stringify({heading: h ? h.textContent : null, navLabel: (function(){ var a = document.querySelector('#sideNav a[href=\"#concept-Recebimento\"] .npdev-shell-nav-label'); return a ? a.textContent : null; })(), bodyHasNewLabel: document.body.innerText.indexOf('LC-C2 fast-path test') >= 0});",
      "label": "Read the panel heading + nav label + whether the new label text appears anywhere on the page"
    },
    { "action": "goto", "url": "http://127.0.0.1:8100/npdev-workbench/RecebimentoWorkbench.html", "label": "Open the dedicated Aggregate Workbench page for Recebimento" },
    { "action": "waitForTimeout", "timeoutMs": 2500, "label": "Let the selection view load" },
    { "action": "screenshot", "name": "shot_lc_c2_workbench_after_hotswap", "label": "RecebimentoWorkbench.html after the hot swap" },
    {
      "action": "evaluate",
      "name": "workbench_title_text",
      "script": "return JSON.stringify({documentTitle: document.title, bodyHasNewLabel: document.body.innerText.indexOf('LC-C2 fast-path test') >= 0, bodyStart: document.body.innerText.slice(0,200)});",
      "label": "Read the workbench page title + whether the new label appears"
    },
    { "action": "collect", "what": ["domText", "console", "url"] }
  ]
}
