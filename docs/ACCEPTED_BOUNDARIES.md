# Accepted Boundaries — what NPDev deliberately does not do

> **Written:** 2026-07-25 · Verified against the live tree. **Re-verified and reclassified 2026-08-04
> (REG-135)**: every row below was traced to its real enforcing code (or confirmed to have none),
> not just read by its title.
>
> **These are not gaps, not bugs, and not a backlog.** Each is a conscious design decision with a stated
> reason, a workaround, and a named trigger that would justify revisiting it. Treating one of these as a
> "TODO" and "fixing" it without hitting its trigger is how a stable subsystem gets destabilised.
>
> **How to use this document:** before filing an item as a gap, check here. If it is listed, the question
> is not *"why isn't this done?"* but *"has its revisit trigger fired?"*
>
> **This document is user-facing** — real product limits an app author or end user can hit while
> building or running an app. Six rows that used to live here (B21-B26) were about NPDev's OWN CI,
> corpus layout, and test-coverage practices, not the product — moved to
> `docs/PROJECT_POSTURE.md` (REG-135 step 2). A boundary marked **HITTABLE** below fires a real
> diagnostic (rendered as `LIMIT`, not `ERROR`, when the mechanism reaches it — see
> `ledger/boundaries/*.yml` for which ones are wired to a `boundaryId` today); one marked
> **POSTURAL** is a real, documented limit with no diagnostic to fire.

## Classification (REG-135 step 1, verified against real code, not row titles)

| Kind | Rows | Notes |
|---|---|---|
| **HITTABLE** | B1, B2, B4, B5, B10, B13, B16, B28, B30 | A user action reaches a real refusal/diagnostic/workflow gate. **B9, B17, and B19 moved to LIFTED 2026-09-02** (STOR-18 / SEC-8 / EDIT-17, `BOUNDARY_LIFT_PLAN_2026-09-02.md` wave 1); **B8 moved to LIFTED 2026-09-02** (STOR-19, wave 2) -- see the LIFTED row below. **Every row in this list now carries a real, discoverable code** (`ledger/boundaries/B*.yml`, each own `codeLinkedNote`) -- this took two passes to get right (W2.3, W2.4a, W2.5, W5.5), and REG-135's original "only B16 is wired" (2026-08-04) was stale well before this session started: a "Wave 2" commit (`50ddf3ea`, 2026-08-21) had already wired B2 (REST precondition only)/B4/B5/B9/B10's boot/REST refusals, landing BEFORE the 2026-08-23 audit that produced the 2026-08-25 remediation plan -- that audit read the stale 2026-08-04 notes without re-tracing the live code (the same stale-premise shape B19 had) and reported all six as unwired. **Four DIFFERENT code-carrying shapes are now in use, deliberately not unified into one** (each `codeLinkedNote` says which): (1) B1/B13/B16/B19 prefix a DSL `ValidationDiagnostic`'s message with `B<n>:<code>:`, stripped by `ValidationDiagnosticNormalizer.BOUNDARY_PREFIX_IDS`; (2) B2/B4/B5/B9/B10 prefix a `BoundaryViolation`'s message the same way, but UNSTRIPPED -- `BoundaryViolation` has no separate `code` field, so the prefix survives into both the boot log and the REST JSON body `BoundaryViolationResponse` writes; (3) B17 is a literal hand-written JSON string carrying a genuine separate `code` field; (4) B28 is a structured `{"ok": false, "code": ..., "message": ...}` body via `MetadataHotSwapController`'s own `failure()` helper (same shape its `/apply` endpoint already used). B8 is the one exception with no shape at all -- not a refusal (boot continues, the drop is silently skipped), so its code lives on a warning log line only. **B30 (2026-08-27) uses shape (2)** -- a `BoundaryBootException` carrying `B30:plugin_bytecode_violation:` -- the fifth HITTABLE row to do so. Extending `BoundaryViolation` with a real `code` field, so a future boundary does not need a fifth ad-hoc shape, is real follow-up work, not attempted this session.
| **POSTURAL** | B3, B6, B7, B11, B12, B14, B18, B29 | Real limits, nothing to hit -- no diagnostic fires, so no `boundaryId`/`LIMIT` mechanism applies. **B19 moved to HITTABLE 2026-08-25** (remediation plan W2.5) -- it had been misclassified here since REG-135: `PanelValidation.validateBandPickers` was re-traced and found to have ALWAYS required `panel` unconditionally (`else if (!hasText(entry.getValue().panel())) { errors.add(... "panel is required"); }`), not merely "checks band NAMES" as this row previously claimed -- a no-panel band picker was never silently inert, it was always a hard refusal; the only real gap was the refusal carrying no `boundaryId`, now fixed. See `ledger/boundaries/B19.yml`'s `codeLinkedNote` for the full trace.
| **LIFTED** | B8, B9, B15(A), B15(B), B17, B19, B20, B27 | Closed boundaries, kept for history (8 rows, 7 boundary numbers -- B15 spans two rows). Each still reads LIFTED in its own text, re-confirmed.
| **INFRASTRUCTURAL** | *(moved out)* | B21-B26 -- see `docs/PROJECT_POSTURE.md`. |

---

## 1. Schema evolution & migration

| # | Boundary | Why it is deliberate | What to do instead (workaround) | Revisit trigger | Ref |
|---|---|---|---|---|---|
| B1 | **No automatic rename inference** — a rename must be declared via `renamedFrom` (Move 9 B2: the editor now declares it for you; boundaries-2026-08-12 B1.1-B1.3: the refusal itself is now actionable and the hand-edit path has its own declaring command; boundary lift plan 2026-09-02 package 2.2 / REG-197: the suggestion is now ranked, scored, and one command from accepted) | In a pure shape diff, "renamed a→b" and "dropped a, added b" are **identical**. Guessing wrong silently destroys a column's data. Intent cannot be recovered from shape — this stays true forever, by design; nothing below infers anything, ever. A confidence score computed from the same shape that cannot distinguish the two cases is 96% confidence in a heuristic, not in the author's intent — so a score orders candidates for a human to judge, it never authorizes one | Renaming a field in `NPDevEditor`'s Field Details panel still stamps `renamedFrom` on the saved field automatically (`updateField` in `editorUtils.ts` — the single choke point every field-name edit passes through). For a hand-authored or directly-edited model.json: (1) the boot refusal itself still names the fix, not just the rule — `ImpactReportText#appendPossibleRenames` (B1.1) renders every candidate scoring at least half of `RenameCandidateScorer.MAX_SCORE`, with every contributing signal shown, above the DESTRUCTIVE_DROP_COLUMN-with-live-data / pending-ADD_COLUMN pairing B1.1 always used; (2) **`npdev migration diff --suggest-renames`** (REG-197, package 2.2) runs the SAME shared scorer (`com.npdev.dsl.v1.schemaevolution.RenameCandidateScorer` — table, type equality, nullability, default, unique membership, FK target, ordinal position, and normalized name distance, never name alone) against a model-vs-model diff (no live database, no boot needed) and prints every ranked candidate unfiltered, each resolved to a `Concept`/field name when the generator can reverse-map the SQL identifiers; (3) **`npdev migrate rename --model <path> <Concept>.<oldField> <newField> [--write]`** (B1.2) still works for a single field, and **`npdev migrate rename --from-suggestions <file> [--write]`** (REG-197) applies every accepted candidate from a `--suggest-renames` report in one pass, through the identical stamping logic — dry-run by default, `--cascade` not supported in the batch form (apply it per field afterward if references need rewriting). Nothing is ever applied without an explicit accept: no confidence threshold auto-applies anything | Closed for the editor-authored path (unchanged). For the hand-edited-JSON path: the underlying limitation is not liftable and never will be (see "why" above) — what changed is that hitting it is no longer a dead end: the refusal names the exact fix, evidence is ranked instead of all-or-nothing, and one command applies an accepted batch. An opt-in `--accept-above <score>` for disposable dev databases only (refusing against anything but the local dev engine) was scoped in the same plan package but deliberately deferred — a live-database-write safety feature warranting its own review, not required for the dead end itself to be gone | `DATABASES_AND_MIGRATIONS.md` §15, Move 9 B2, `NPDev_General__OutsideRepo/boundaries-2026-08-12/PLAN.md` B1, `NPDev_General__OutsideRepo/BOUNDARY_LIFT_PLAN_2026-09-02.md` package 2.2, `RenameCandidateScorer.java`, `ImpactReportText#appendPossibleRenames`, `MigrationPlanEmitter#diffColumns`, `ModelChangeClassifierMain#enrichRenameCandidatesWithFieldNames`, `npdev_cli.py#run_migrate_rename` / `_run_migrate_rename_from_suggestions` |
| B2 | **Expression-valued backfills require an explicit dry-run + acknowledgment** (Move 9 B1) — no longer refused outright; a SAFE-classified expression no longer needs the acknowledgment at all (REG-202, package 3.1, 2026-09-03) | REG-7's revisit trigger fired: `ExpressionBackfillPreview` now evaluates every pending expression-default column against the CURRENTLY LIVE rows (same `ValueExpressionEvaluator` a new row's `defaultExpression` already uses at write time — no new execution risk introduced), reporting rows affected, up to 20 distinct resulting values, and any row where evaluation produces no value (which would violate the NOT NULL constraint about to be added). Surfaced read-only via `GET /api/admin/schema-migration/expression-backfill-preview`; applied by `BackfillPass` only once an operator submits the SAME `ackToken` via the EXISTING `/acknowledge` endpoint (no new channel), and only if a FRESH re-evaluation right before writing still has zero failing rows. The unacknowledged boot refusal now carries `B2:expression_backfill_requires_ack:` as a message prefix (2026-08-25 W2.3), so it is discoverable in the boot log rather than only in English. **Package 3.1 (REG-202) adds risk-tiered auto-apply on top, the ack mechanism itself unchanged:** `ExpressionBackfillRiskClassifier` (NPDevKernel, reusing `ComputedExpression`'s own parser via a new `functionCalls` traversal) classifies each candidate's expression TEXT as SAFE (a constant, `now()`/`uuid()`, a single-column `$field` copy, or a pure same-row arithmetic/comparison/logical expression with no function call at all), REVIEWABLE (any function call), or HIGH_RISK (a `scope.*`-prefixed call — the one concrete cross-record signal the grammar carries). A SAFE candidate whose Pass-1 evaluation already shows zero failing rows and a row count at or under `npdev.schema.backfill.auto-apply-max-rows` (default 10000) auto-applies on boot with a loud, itemized `AUTO-APPLIED` log line naming exactly what ran — no operator proves what the platform can already prove itself. `npdev.schema.backfill.auto-apply=none\|safe\|safe-and-reviewable` (default `safe`) gates which tiers are eligible; the chosen mode is printed at boot whenever an expression-default candidate exists. HIGH_RISK never auto-applies under any mode — that ladder rung deliberately does not exist | Call the preview endpoint — each item now also carries `riskTier`, `autoApplyEligible`, `nullabilityChange`, and the ordered DDL `steps` it will run — review the distinct values and any `failedRowIds`, then acknowledge via the existing endpoint for anything REVIEWABLE or HIGH_RISK (or SAFE but over the row threshold/with a failing row). A field with rows that fail evaluation (a referenced field absent from some rows) still cannot be backfilled — declare a literal `default`, make the field optional, or write a conversion hook instead. Tune auto-apply with `-Dnpdev.schema.backfill.auto-apply=<mode>` and `-Dnpdev.schema.backfill.auto-apply-max-rows=<n>` | Closed for the single-column, single-expression case, and now for the SAFE tier's acknowledgment step too. Multi-column expressions that reference EACH OTHER's about-to-be-backfilled values are still out of scope. `HIGH_RISK`'s only concrete signal today (`scope.*`) is unreachable via `defaultExpression` in practice (no `FunctionRegistry` is ever wired for this evaluation path) — the tier exists for the grammar's own sake and to fail closed if that ever changes | §15, Move 9 B1, `BOUNDARY_LIFT_PLAN_2026-09-02.md` package 3.1, REG-202, `ExpressionBackfillRiskClassifier.java`, `ComputedExpression#functionCalls` |
| B3 | **FK/index checking is missing-only for DDL purposes — surplus is now REPORTED (advisory-only), never acted on.** Surplus classification attempted S8 Wave 2 (2026-08-04) and its own hard stop fired; boundaries-2026-08-12 (B3.1-B3.4) re-ran the calibration, found a SECOND blocker the first pass missed, fixed it, and wired the result in read-only | The live side always carries constraints the model never declares (implicit PK/unique indexes, a DBA's performance indexes). Reporting extras as something to fix would propose **dropping primary-key indexes** — the reasoning above stands, which is WHY a classifier (not a wholesale surplus report, and never a drop path) is the right shape. `ConstraintSurplusClassifier` (15/15 vectors from `b3-classification-vectors.json`, including the two — H2's `PRIMARY_KEY_5`, Postgres's `orders_pkey` — that pin the exact failure this boundary exists to prevent) classifies a live constraint into platform-declared/implicit/foreign/unclassifiable BY STRUCTURE, never by name. **Two real blockers stood between "classifier exists" and "safe to report," found only by re-measuring against WmsOffice's real accumulated schema, not by reasoning about the code:** (1) REG-129 (closed 2026-08-04) — `businessTableIndexes` never enumerated LNCH-6's implicit panel/query-driven secondary indexes at all, so all 17 of WmsOffice's real `idx_<table>_<field>` indexes were invisible to the classifier and misreported FOREIGN. (2) **REG-145 (found 2026-08-12, re-measuring AFTER REG-129 shipped, expecting zero findings and getting the same 17)** — REG-129's own fix enumerated the right indexes but recorded the wrong COLUMNS: `[field]` instead of the DDL's actual tenant-composite `[tenant_id, field]`. `ConstraintSurplusClassifier`'s match is column-ORDER-sensitive, so a live `(tenant_id, field)` index could never match a declared `[field]` entry — REG-129's own live verification had compared "by column set, tenant_id-stripped," which is exactly loose enough to hide this. Re-running the SAME calibration after REG-145's fix: all 17 of WmsOffice's real live secondary-index shapes now match the (corrected) manifest's declared shape EXACTLY, column-for-column, order and uniqueness both — confirmed by direct comparison of the regenerated manifest against the original live-index evidence (a full app-boot re-confirmation was attempted but repeatedly hit unrelated environment friction — see Ref; the manifest-vs-live-shape comparison is deterministic and does not depend on booting the app) | Manage extra indexes outside NPDev; they are respected and never touched — true whether or not surplus reporting exists, since there is still no drop path, not even a stub. **Now also:** `ImpactReport` (boot log, `-ImpactOnly`/`REPORT_ONLY` CLI, and the ControlPanel `GET /api/admin/schema-migration/impact` — all three share one renderer) carries an informational, always-advisory surplus section — present only when there is something to report, and it NEVER changes `verdict` and NEVER proposes a drop. **Deliberately no gate**: a gate that fails a build on surplus would turn a DBA's performance index into a build failure, which is a materially different, larger decision than reporting it — not attempted here | REG-129's fix widened `collectIndexes` to enumerate LNCH-6 indexes; REG-145's fix corrected the column list it records for them (`List.of("tenant_id", column)`, matching `appendSecondaryIndexes`'s actual DDL). B3.2 threads `SchemaDiffEngine#findSurplusConstraints` (same `desired`/`current` pair the ordinary diff already computes — zero extra DB round-trips) into `SchemaImpactFacade.Result#surplus` and both `ImpactReportText`/`ImpactReportJson` renderers | §15, SER-G8, REG-129, REG-145, `NPDevRuntimeHost/.../db/schemastate/ConstraintSurplusClassifier.java`, `.../SchemaDiffEngine.java#findSurplusConstraints`, `.../SchemaImpactFacade.java`, `NPDevGenerator/.../dbconfig/SchemaRealizationEmitter.java#collectIndexes`, `__OutsideRepo/wave2/b3-wmsoffice-calibration.txt` (original 17-phantom evidence), `__OutsideRepo/boundaries-2026-08-12/b3-wmsoffice-calibration-rerun*.{txt,json,md}` (re-run evidence, both pre- and post-REG-145), `NPDev_General__OutsideRepo/boundaries-2026-08-12/PLAN.md` B3 |
| B4 | **Multi-instance migration is a real lock, not detect-and-refuse — now with a one-shot production mode and a progress-aware wait** (R9.3; migrate-only + adaptive wait, REG-200, 2026-09-03) | REG-7's revisit trigger fired (a real multi-instance rolling-deploy requirement). **R9.3 replaced the original detect-and-refuse mechanism entirely** (this row described that pre-R9.3 shape until this update, which corrects it): `MigrationMutex` now WAITS rather than refusing on sight, by CAPABILITY not engine name — Postgres/MySQL/SQL Server take the engine's own session advisory lock (needs no table, so it protects a genuinely fresh database with no special case); H2 takes a row lock (`SELECT ... FOR UPDATE SKIP LOCKED`) on a single row in a DEDICATED schema (`npdev_lock`) that Flyway never inspects — closing the old fresh-database gap for the LOCK itself on every engine, not just three of four. Both mechanisms are connection-scoped: a crashed holder releases automatically when its socket dies, with no lease, no heartbeat, no operator step (a HUNG, not crashed, holder is a different case — see below). `MigrationClaimStore`'s human-readable claim row (bookkeeping only, never load-bearing for exclusion since R9.3) is unchanged in shape. A boot that waits out its budget refuses with `B4:migration_lock_held:` as a message prefix (2026-08-25 W2.3). **REG-200 adds two things on top, the wait/refuse mechanism itself unchanged:** (1) `npdev.schema.lifecycle.mode=MIGRATE_ONLY` (`Migrate-Only.ps1` / `Build-NpdevApp.ps1 -MigrateOnly`) runs the schema lifecycle once, for real, then exits before the web server binds a port — the actual production answer, since a one-shot job run before any serving instance boots means nothing ever contends this lock. (2) The wait is now progress-aware: `MigrationMutex.acquire` keeps `waitSeconds` as a FLOOR but extends past it while `SchemaHistoryStore.mostRecentActivity` (reading the pre-existing `npdev_schema_history` write-before-execute rows every rename/widen/relax/backfill pass already writes) shows new activity since the wait began; a stalled holder still refuses at the floor, now naming its last recorded activity or its absence | Run migration as a one-shot `MIGRATE_ONLY` job before any serving instance boots, so nothing ever contends the lock. If two instances do race anyway, the second waits under the real lock — automatically extending while `npdev_schema_history` shows genuine progress — and refuses naming the holder (plus its last recorded activity) if the holder stalls; a HUNG (not crashed) holder still blocks until killed or the budget is raised with `-Dnpdev.schema.lock.waitSeconds`, by design (a migration has no safe maximum duration NPDev can know in advance). REG-7.2's narrow residue — the claim/history bookkeeping row is unrecorded on a genuinely virgin database's first-ever boot — is a deliberate, named limit (`MigrationClaimStore`'s own javadoc), mitigated by `MIGRATE_ONLY` rather than a new file-lock sidecar: it only bites when two boots race a first-ever migration against each other, which a correctly-ordered deployment using `MIGRATE_ONLY` makes unreachable | None identified for the lock itself (closed on every engine, including a fresh database, since R9.3). REG-7.2's bookkeeping-row residue would need a dedicated-schema rewrite of `MigrationClaimStore`/`SchemaHistoryStore` to close outright — not attempted, since `MIGRATE_ONLY` already removes the scenario it matters in | REG-7 §1.7, Move 9 A1, REG-200, `ledger/boundaries/B4.yml`, `MigrationMutex.java`, `MigrationClaimStore.java`, `SchemaHistoryStore.java` |
| B5 | **Schema-ahead detection closes by refusal, not reconstruction — with an itemized diagnosis, and reverse migration for the pure-superset case** (B5-A, REG-198; B5-B, REG-199; 2026-09-02) | You cannot detect what was *removed* without history. REG-8 Trigger C refuses when the DB was migrated past this build, which is the safe answer. The refusal carries `B5:schema_ahead_detected:` as a message prefix (2026-08-25 W2.3), discoverable in both the boot log and the 503 JSON body. **B5-A**: `SchemaLifecycleExecutor.afterMigrate` now stores a full desired-schema snapshot per fingerprint (`npdev_schema_snapshot`, keyed so it travels with the database, not the filesystem) in the SAME connection block that already writes the fingerprint. When the ahead-detector fires, `SchemaAheadAnalysis` diffs THIS build's own desired schema against the ahead fingerprint's recorded snapshot using the SAME `SchemaDiffEngine`/`SafetyClass` vocabulary the Impact Report and rename suggester already use, and classifies each difference into one of three operator-facing buckets: this build can proceed ignoring it / this needs the newer build / this would require a destructive downgrade. The itemized list is appended to both the boot refusal message and the 503 JSON body; `npdev db schema-ahead --report` (`SchemaAheadMain`) renders the identical analysis with no app boot required. A database that reached the ahead fingerprint before this shipped, or via a mark-done fast-forward (neither writes a snapshot), degrades to a plain explanation instead of an itemized one. **B5-B** turns that diagnosis into an action for the PURE-SUPERSET case: `ReverseMigrationPlanner` diffs a FRESH live schema (never the stored snapshot, which stays advisory-only) via the same plumbing the Impact Report uses, and refuses the whole plan outright — never a silent partial reversal — if any item needs the newer build (ambiguous, e.g. a rename can look like an unrelated drop-plus-add) or is a type-narrowing item (out of scope per the package's own done-when, since narrowing can truncate data unlike a straight drop). A Ready plan executes behind the exact same `DestructiveAckToken` function the forward path uses, reusing `DestructiveRecreationPass`'s DROP primitives and `SchemaDropSnapshotWriter`'s pre-drop snapshot unchanged; clearing the ahead-gate needed no new fingerprint-writing plumbing, since the gate itself only reads `npdev_schema_history`'s latest row. `npdev db reverse-migrate --preview` / `--ack-token <token>` (`ReverseMigrateMain`) mirrors `db schema-ahead`'s no-boot CLI shape. **Any refusal that isn't a pure-superset ahead diff is unchanged and still fires in every case** — nothing here silently reverses an ambiguous or type-narrowing diff, or runs DDL without an explicit, freshly-verified token (`--schema-ahead-mode=IGNORE` was explicitly rejected) | Roll forward, use `mark-done` deliberately after verifying by hand, or — for a pure-superset ahead diff only — `npdev db reverse-migrate --preview` then `--ack-token <token>` to step the database back down, all informed by the itemized diagnosis | None identified. Ambiguous and type-narrowing diffs are a deliberate, permanent limit (not a gap the package left open) — reversing either would require guessing at, or truncating, data a human should review by hand | REG-8 §1.8, REG-198, REG-199, `ledger/boundaries/B5.yml`, `SchemaAheadAnalysis.java`, `SchemaSnapshotStore.java`, `SchemaAheadMain.java`, `ReverseMigrationPlanner.java`, `ReverseMigrateMain.java` |
| B6 | **`mark-done` trusts the operator completely** | It exists precisely for "I fixed it by hand; stop trying to converge". Verifying would defeat its purpose | Use `-ImpactOnly` first to see what NPDev *thinks* differs, then mark done knowingly | Never — verification is what the ordinary path already does | §15 |
| B7 | **Refusals are not side-effect-free** | A refusal can happen after an idempotent Flyway step already ran. Making the whole boot transactional is impossible across engines (H2 has no transactional DDL) | Treat a refused boot as "inspect before retrying"; the history table records what ran | Only if every supported engine gains transactional DDL | §13, §15 |
| B8 | ~~Dropping a concept needs one boot of ownership history first~~ (LNCH-1-B7) **LIFTED, by construction — STOR-19, 2026-09-02** | NPDev only drops a table it can **prove** it created — that invariant is unchanged and permanent. What closed: two ways the proof could go missing. (a) A table THIS boot's own migration just created, if a later same-boot pass (backfill, unique-constraint tightening) then refused — ownership used to be written only once, at the very end of `afterMigrate`, after both of those passes; `SchemaLifecycleExecutor.recordOwnershipForLiveManifestTables` now writes it FIRST, so a later refusal in the same boot can no longer leave a just-created table without a record (the fingerprint write stays gated behind those passes, unchanged, for its own separate reason: a refusal must leave it stale so the next boot re-attempts). (b) A genuinely legacy table that predates ownership recording — `OwnershipAdoption` (new) + `GET/POST /api/admin/schema-migration/ownership/{preview,adopt}` let an operator adopt it explicitly, but ONLY when its live shape matches the model with zero diff (same `SchemaDiffEngine` `classify()` uses) — never inferred, never for a same-named table that does not match exactly. `WARNING [B8:no_ownership_history]` (unchanged code) still fires, now only for a table genuinely never created by this build and never adopted | `npdev db adopt-ownership [--apply]` against a running app (preview by default). The original "boot once with the concept present" path still works unchanged | — (closed) | §15, STOR-19, `ledger/boundaries/B8.yml`, `OwnershipAdoption.java`, `OwnershipAdoptionTest.java` |
| B9 | ~~Snapshot restore is operator-driven, one (snapshot, table) pair at a time — never "restore all"~~ **LIFTED, with a preflight gate — STOR-18, 2026-09-02** | REG-7's revisit trigger fired (2026-08-25): `SchemaDropSnapshotRestorer` compares a named snapshot's rows against the CURRENTLY LIVE table by `id` and classifies each as insert / already-present-identical / **conflict** (present live with DIFFERENT content). `GET .../snapshots/{snapshot}/tables/{table}/preview` is read-only; `POST .../snapshots/{snapshot}/tables/{table}/restore` only INSERTs the missing rows — a conflicting row is reported but **never overwritten**, and applying refuses outright if the live table does not exist yet, carrying `B9:single_table_scope:` (unchanged). **What STOR-18 (wave 1 of `BOUNDARY_LIFT_PLAN_2026-09-02.md`) closed**: the row's own revisit trigger, above. `POST .../schema-migration/snapshots/{snapshot}/restore` (new, distinct path from the unchanged per-table endpoint) accepts an explicit table list or `{"tables":["all"]}`/an omitted body, preflights EVERY named table's live existence before writing anything (`B9:batch_preflight_failed:` if any is missing — nothing written), restores parent tables before FK-dependent children (`SchemaDropSnapshotRestorePlan`, pure topological sort over the manifest's declared foreign keys), and delegates each table to the SAME unchanged `apply()` — a conflict in one table does not stop the others, reported per table plus an overall `anyConflicts`. `npdev db restore --snapshot <name> [--all \\| --tables a,b] [--apply]` previews by default (writes nothing without `--apply`, mirroring `/promote/preview` vs `/promote/apply`) | Call the batch endpoint directly, or `npdev db restore --snapshot <name> --all --apply` (omit `--apply` to preview first). The original single-table preview/restore endpoints are unchanged for a single (snapshot, table) call | — (closed) | §13, §15, Move 9 B3, STOR-18, `ledger/boundaries/B9.yml`, `SchemaDropSnapshotRestorePlan.java`, `SchemaDropSnapshotRestorePlanTest.java` |
| B10 | **H2→Postgres (or any engine pair) promotion is a real, ONE-COMMAND, operator-driven arc — schema realization included, SQL translation still out of scope** (Move 9 A4; one-command arc + deep verification, REG-201, package 3.3, 2026-09-03) | REG-87's product question was asked and answered: prototype-on-H2-then-promote-to-Postgres IS a chosen NPDev arc, not just an adapter-pair artifact. `npdev db promote --app <dir> --to <jdbcUrl> [--dry-run]` (`PromoteMain`, no app boot required) now runs the WHOLE arc: realizes the target schema (new — reuses package 3.2's MIGRATE_ONLY mechanism in-process, a real `Flyway` instance against the target running the SAME generated `classpath:db/schema-realization` migrations a normal boot would; the target's dialect is derived from its own CONNECTION, never the source manifest, closing a real latent bug this package found — a cross-engine promotion would otherwise have pinned the SOURCE engine's DDL guard syntax against the TARGET connection), previews, applies (unchanged — `CrossEngineDataPromotion` copies rows only, typed per column via the SAME `SchemaManifest.businessTableColumnTypes()` map diffing/backfill already use; a JSONB column round-trips through `SchemaDropSnapshotWriter`'s existing decode logic and back out as a real Postgres `jsonb`), then verifies DEEPER than row counts (new, `PromotionVerifier`: per-column null counts, an order-independent per-table content hash — a per-row hash SUMMED, not chained, so read order cannot flip the verdict — and FK/index/shape parity, reusing `ExternalSchemaVerification.findExternalSchemaIncompatibilities` rather than a second copy of that comparison). Prints `PROMOTION VERIFIED` or an itemized list of exactly what did not match — never a bare "Postgres started successfully". `PromotionVerifier` is CLI-only for now (called from `PromoteMain`), deliberately not mirrored as a REST endpoint — see `SchemaAcknowledgmentController`'s own comment on why. Takes A1's migration claim (the REAL `release`, not the `clear` escape hatch) on the target for the duration | Run `npdev db promote --app <dir> --to <jdbcUrl> --dry-run` first (zero writes, even if the target has no schema yet), then without `--dry-run` to realize, copy, and verify in one command. Still no shape RECONCILIATION for a target realized outside this command's own step — a target table that doesn't exist yet still refuses with `B10:data_only_promotion:` as a message prefix if you call the bare REST endpoints directly (which still assume the target already exists) rather than the CLI. A failed table's copy does not abort the rest of the promotion (QUAL-38, closed 2026-08-26; the regression-proof gap in that fix's own test coverage — Testcontainers-Postgres-gated, single-table only — is now closed too, by a locally-runnable H2-only test) | H2-SQL→Postgres-SQL translation — deliberately out of scope; every DDL statement realized on the target is the SAME generated, engine-specific `V1__` migration the app's own boot would run, never a hand-translated one, matching the boundary's own original framing | §15, Move 9 A4, REG-87, REG-201, `ledger/boundaries/B10.yml`, `PromoteMain.java`, `PromotionVerifier.java` |
| B11 | **H2 (and MySQL, STOR-2) have no transactional DDL** — a failed hook `verifySql` rolls back DML but not DDL. boundaries-2026-08-12 B11.1-B11.3 + package 3.4 (STOR-20, 2026-09-03): the blast radius, not the behaviour, is what improved, and the mixed shape that exposes the limitation no longer has to be authored blind — see below | An engine limitation, not a code choice. Postgres and SQL Server roll back fully | **Warned at the point of risk, before it happens (B11.1):** when a conversion hook mixes DDL with a `verifySql` on an engine without `DDL_IN_TRANSACTION` (`SqlDialects.active().supports(StorageCapability.DDL_IN_TRANSACTION)`, asked rather than hardcoded to `"h2"` — the old hardcoded check would have MISSED MySQL and wrongly fired on SQL Server), `ConversionHookRunner` prints a WARNING naming the real active engine and the remedy before running it. **The remedy is also in the actual failure message, not just the doc (B11.2):** `PartialApplicationTruth.afterRollback()`'s implicit-commit branch — the one call site every such refusal already routes through (STOR-2) — now appends "split destructive DDL and data movement into separate hooks/boots ... see docs/ACCEPTED_BOUNDARIES.md B11" to the truthful diagnosis, so an operator reading the actual error (not just a warning they may have scrolled past) sees the fix. **Rule 5 (post-hook re-diff) is confirmed to run on the H2 path (B11.3)** by `ConversionHookRunnerH2Test#rule5_claimedButNotActuallyResolvedRefusesTheBoot`, against a real H2 database — it is the engine-independent backstop regardless of which engine's DDL commits when. **Package 3.4 (STOR-20) lifts the shape, not the engine limit:** the SAME detection now REFUSES the boot outright, BEFORE the hook runs at all (never a partial apply), carrying `B11:mixed_ddl_verify_refused:` as a message prefix, once an operator sets `-Dnpdev.schema.conversionHooks.mixedDdlVerify=refuse` — an opt-in property, default `warn` (today's B11.1 behaviour, unchanged), so no existing app breaks without notice on upgrade; flipping the default to `refuse` is a deliberately deferred follow-up. The refusal names a concrete two-hook split (DDL-only, no verifySql, then data-movement-with-verify, run in the existing ascending-id order per rule 3) as a SUGGESTION rather than attempting to split the hook's SQL automatically — a DDL-journal-style auto-split was explicitly rejected platform-wide (`BOUNDARY_LIFT_PLAN_2026-09-02.md` §6) as executing generated DDL at the moment state is least certain, the higher-risk move, not the safer one | Never, for the engine limitation itself (upstream H2/MySQL behaviour — this half of the row can never be lifted). **Closed, for the authoring shape (package 3.4, STOR-20):** an operator who opts into `mixedDdlVerify=refuse` can no longer accidentally author (or re-run) a hook in this dangerous shape — the boot refuses first, with the fix named in the message. **Open, owner-only (B11.4, deliberately not answered here):** should H2 (file) remain the zero-prerequisite default engine even though it is the one engine where a failed conversion hook can leave DDL applied and DML rolled back? That is a product tradeoff between "no prerequisites" and "conversion hooks are atomic," not an engineering question this row can resolve | `docs/archive/programme-history/IMPACT_REPORTS.md`, `ConversionHookRunner` javadoc (the `MIXES_DDL_PATTERN` warning/refusal), `PartialApplicationTruth#afterRollback`, `PartialApplicationTruthTest#implicitCommitEngineFailureMessageCarriesTheRemedy`, `ConversionHookRunnerH2Test#b11_1_*`/`#rule5_*`/`#mixedDdlVerify*`, `NPDev_General__OutsideRepo/boundaries-2026-08-12/PLAN.md` B11, `BOUNDARY_LIFT_PLAN_2026-09-02.md` package 3.4, STOR-20 |
| B12 | **Conversion hooks are individually atomic, not collectively atomic** | Each hook runs in its own transaction so a later failure does not silently undo verified earlier work | **Write every hook idempotent** (`ADD COLUMN IF NOT EXISTS`, `UPDATE … WHERE <not-yet-converted>`) | A single-transaction multi-hook mode, if operators ask for it | `docs/archive/programme-history/IMPACT_REPORTS.md` |
| B13 | **Conversion hooks are SQL-only** — no Java `DataMigrationHook`. **A declarative vocabulary now exists BESIDE the raw-SQL path** (S7 Phase B, 2026-08-03; widened to 5 ops — S8 W1.2, 2026-08-04; widened to 7 ops — Wave 4 package 4.2, REG-203, 2026-09-03): `conversions[]` (`copy`/`split`/`lookup`/`merge`/`convert`/`coalesce`/`case`) lets an author express a schema-evolution data migration without writing SQL by hand; the compiler resolves every `concept`/field reference against the real model graph (an unresolvable one is a named compile error, never a silently-dropped conversion) and the generator compiles each entry to the SAME `db/conversion-hooks/<id>/{hook.json,convert.sql}` shape `ConversionHookRunner` already executes — one execution path, not two. Every generated hook is idempotent BY CONSTRUCTION (`ADD COLUMN IF NOT EXISTS` + `UPDATE … WHERE <col> IS NULL` + a re-runnable `ALTER COLUMN … SET NOT NULL`), turning B12's "write every hook idempotent" discipline into a compiler guarantee for this path. Scope, deliberately: v1 covers a target field that is BRAND NEW in this model version (`ADD_REQUIRED_COLUMN`) — a field that already exists and merely needs data migrated converges as SAFE_ADDITIVE/NEEDS_BACKFILL with no residual diff item for a hook to claim, so it never reaches this mechanism at all (use `defaultExpression`/`ExpressionBackfillPreview` — B2 above — instead). A row a conversion cannot populate (e.g. `split` with no space in the source value, `merge` with either source field NULL) is left NULL and the closing `SET NOT NULL` then fails the boot loudly with the engine's own error, never a silent partial conversion. **`merge` and `convert` (S8 W1.2, roadmap deferred item #4) reuse this SAME `ADD_REQUIRED_COLUMN` shape, not a new one**: `merge` is `split`'s inverse (N declared source fields, `CONCAT`-ed with an author-declared separator, into one new field); `convert` is `copy` with an explicit `CAST` to the target field's own declared type instead of a bare assignment — a source value the target type cannot represent fails the `CAST` itself (a real `SQLException`, rolling back the whole hook transaction) rather than leaving a partially-typed column. Neither op has (or needed) a `toType`/separate-type property: `convert`'s target type is `to`'s own already-compiler-resolved declared field type, the same information a second `toType` property would just duplicate and risk drifting from. **Wave 4 (package 4.2, REG-203, 2026-09-03) adds two more, both plain ANSI SQL needing no dialect-specific emission (`ConversionHookEmitter` stays inside STOR-1's one dialect-bound-SQL package regardless):** `coalesce` reuses `merge`'s `mergeFrom`/`to` shape but picks the first NON-NULL source (`COALESCE(...)`) rather than concatenating every source — no `with` separator, since nothing is joined, and (unlike `merge`) no "every source non-null" WHERE guard, since COALESCE's whole point is tolerating NULLs; a row where every source is NULL still yields NULL and the closing `SET NOT NULL` fails the boot loudly on it, same X0 discipline. `case` maps a source field's literal values to literal replacements via a portable `CASE WHEN ... THEN ... ELSE ... END` — `when` (1+ `equals`/`then` literal pairs, evaluated in declared order, first match wins) plus an optional `else` default; a row matching no `when` clause with no `else` declared is left NULL by the bare `CASE ... END` (no `ELSE` clause emitted), same X0 discipline as every other op. Both ops chose `concat`/`cast` as their plan-sketched names originally, but `merge`/`convert` already cover that exact vocabulary ground (`merge` already IS a portable `CONCAT`; `convert` already IS a `CAST`) — building second, differently-named ops with identical semantics would have been pure duplication, not vocabulary growth, so `coalesce`/`case` were built instead as the two genuinely NEW primitives on the plan's original six-name list. **Deliberately NOT built**: retyping an EXISTING field's OWN column IN PLACE (the plan's originally-sketched `convert` shape, `{"op":"convert","concept":"Order","field":"total","toType":"decimal"}`) — traced during S8 W1.2 to a real architectural wall, not a choice: `ConversionHookRunner.run()` only ever selects a hook whose `claims` intersect the LIVE schema diff's unresolved items, and an in-place retype's correct claim shape is `NARROW_TYPE:table:column:fromType:toType` (`SchemaDeltaItem.NarrowType`), which needs the column's CURRENT live type — unknowable at generation time (the generator compiles one model version, with no "previous version" input). `ADD_REQUIRED_COLUMN:table:column` (a MISSING column) needs no such foreknowledge, which is exactly why `copy`/`split`/`lookup` — and now `merge`/`convert` — all target brand-new fields | **Java hooks remain out of scope, unchanged**: code-bearing objects belong to the ADR-0003 track, with its own sandboxing/versioning story; running author-supplied Java against production data during boot is a materially different and more dangerous risk than compiling a closed, reviewable op vocabulary to SQL, and shipping one casually would pre-empt ADR-0003's design. For the covered case, declare a `conversions[]` entry instead of hand-writing `convert.sql`; for a migration the five ops genuinely cannot express (in particular: retyping an EXISTING field's column in place, see the why-column above), fall back to an operator-authored SQL hook (`definition/migrations/<ordinal>-<slug>/`) as before | Java hooks: ADR-0003 code-bearing-objects lands. Declarative vocabulary: an in-place existing-column retype becomes a real, frequent need — would require plumbing a "previous model version" input into the generator (or a live-DB-aware claim, a bigger change) before it could reuse this same claim-matching mechanism | `ConversionHookRunner` javadoc, `NPDevGenerator/generator/.../dbconfig/ConversionHookEmitter.java`, `NPDevContract/schemas/model.schema.json` `$defs/conversion`, `NPDevSamples/dsl-conformance-max` (corpus witness, all 7 ops), REG-203 |
| B14 | **Sanctioned destruction: a hook-resolved destructive item needs no token** (rule 6) | Authoring the hook *is* the acknowledgment. Owner-approved, ADR-0008 (2026-07-25, no conditions) | Unclaimed destructive items remain fully token-gated; rule 5 verifies every claim by re-diffing | Owner revokes or conditions the ADR | ADR-0008 |

## 2. Authoring & runtime

| # | Boundary | Why it is deliberate | What to do instead | Revisit trigger | Ref |
|---|---|---|---|---|---|
| B15(A) | ~~`await` cannot be nested inside a loop body~~ **LIFTED for the SEQUENTIAL case** (at most one outstanding await at a time) — Move 16 Phase B, 2026-08-03 | Move 9 A5's 2026-07-31 hard stop correctly diagnosed the structural wall for the GENERAL (parallel) case — `FlowInstance` hard-codes one `correlationId`/`currentStepIndex`/`waitingForEventName` per row, `AwaitEventStep`'s wait descriptor lives at one fixed state key (`_npdev.await`) — but never asked the narrower question "what does the SEQUENTIAL-only case (one iteration in flight at a time) actually need?" The answer: nothing new in the schema. `FlowInstance.state` is already a flexible JSON blob; a per-iteration correlation id just OVERWRITES the SAME `state.correlationId` slot before each iteration's body runs (deterministic: `executionId + "::forEach:" + stepName + ":" + iterationIndex` — `FlowStateCodec.deriveForEachIterationCorrelationId`), and a satisfaction marker in that same state map plus an early, mid-iteration checkpoint (pinned to the forEach step's own outer trace index) close the one real crash window (event consumed but outer progress not yet advanced). Two real, previously-unreachable bugs surfaced and got fixed along the way: `ForEachStep`'s nested-failure handling used to unconditionally re-wrap ANY nested failure — including a legitimate WAITING_EVENT — as a generic terminal FAILED; and its `finally` block used to unconditionally wipe the progress/correlation/marker state even when returning early for a genuine wait | Author a `forEach` loop body with exactly one reachable `await` step; validation now accepts it (still rejects two or more reachable awaits in the same loop body — that shape was never exercised by this Move's own restart-proof tests) | — (closed; see B15(B) below, also closed but scoped to a single-step loop body) | `FlowStateCodec.java`, `ForEachStep.java`, `AwaitEventStep.java`, `ResumeCoordinator.java` (`NPDevKernel/kernel/.../`), `FlowValidation.java:470-478` (`NPDevContract/dsl/...`), `KernelRunnerAwaitInLoopRestartProofTest.java` (the required restart proof: brand-new `KernelRunner` instances resuming with events delivered out of order, plus a thread frozen at the exact crash window), `docs/FLOWS.md` §6, Move 9 A5, Move 16 Phase B |
| B15(B) | ~~Parallel awaits inside a loop (N iterations blocking at once) remain unsupported~~ **LIFTED, scoped to a single-step (await-only) loop body — S6 Phase B, 2026-08-03. Widened to a genuinely multi-step body (steps before/after the await) — S8 Wave 3, 2026-08-04, once its own hard-stop restart proof passed** | Move 9 A5's structural analysis was right that B15(A)'s per-iteration correlation id (one slot, overwritten not appended) does nothing for N-in-flight on its own. What closed it (S6): every outstanding descriptor and resolved payload lives in `state` itself, namespaced per iteration (`_npdev.parallelAwait.<step>.<i>`, `__parallelAwaitResolved.<step>.<i>`, `<awaitRef>.<i>`) — `state` already round-trips through persistence in full on every checkpoint, so this needed no new schema/adapter/migration (a `FlowInstance` row stays a single row). The roadmap's originally-costed "new `flow_instance_wait` table" option was found to buy nothing at the discovery layer: all N iterations of one parallel forEach share the same declared await event name, so `FlowInstanceStore.findWaitingByEvent` already finds the candidate instance via its existing indexed column. `ParallelAwaitForEachStep` attempts every unresolved iteration in one pass, checkpointing immediately after each resolves; `ResumeCoordinator` gained a parallel-aware matching path for both the event-driven and scheduled-sweep resume routes. S6 scoped this to EXACTLY one loop step (the await itself) because a step mutating a non-namespaced `state` key would clobber across independently-attempted iterations. **What S8 Wave 3 closed**: `ParallelLoopIterationScope` (I0's design record, in full, on `FlowStateCodec`'s `PARALLEL_LOOP_SCOPE_KEY_PREFIX` javadoc) relocates every write a loop-body step makes into a namespaced shadow key (`__iterScope.<step>.<i>.<key>`) in the SAME `state` blob, diffed against a one-time pre-loop baseline — closing the clobber without changing any of the six previously-unscoped step classes (`ReturnStep`, `ScheduleEventStep`, `EmitEventStep`, `CapabilityCallStep`, `CallProcedureStep`, `MapStep`). I2 decided what survives the loop: everything is scoped-and-discarded except the await's own resolved payload (still folded into one ordered list under `awaitRef`, matching the pre-Wave-3 single-step convention). I3 refuses a `forEach` nested in the body with a named compile error (composing two independent scoping schemes was never attempted) | Author a `forEach` loop body with `"parallelAwait": true` and exactly one reachable `await` step **at the loop body's TOP LEVEL** (not nested inside a `branch` — `FlowValidation` checks this with a non-recursive top-level-only count, deliberately not the same recursive `countAwaitSteps` the plain "at most one await" cap uses, so the validator never accepts a shape `ParallelAwaitForEachStep`'s runtime scan would then reject). Any number of other non-`forEach` steps are now allowed before/after that one await. A `forEach` nested in the body still needs sequential mode (B15(A)) or a flow restructure | — (closed for a flat multi-step body; a `forEach` nested inside a parallel `forEach` — I3 — still needs its own design pass, composing two independent per-iteration scoping schemes was never attempted) | `FlowStateCodec.java`, `ParallelAwaitForEachStep.java`, `ParallelLoopIterationScope.java`, `ForEachStep.java`, `ResumeCoordinator.java` (`NPDevKernel/kernel/.../`), `FlowValidation.java` (`NPDevContract/dsl/...`), `KernelRunnerParallelAwaitInLoopRestartProofTest.java` (extended, not duplicated — S6's two tests plus Wave 3's `multiStepBodyRestartWithIterationsAtDifferentStepsDoesNotReprocessTheResolvedIteration` and `...OutOfOrderDeliveryResumesEachIterationExactlyOnce`), `ParallelAwaitForEachStepIsolationTest.java` (vectors 1/3/4/5/9), `docs/FLOWS.md` §6, `docs/BOUNDARY_LIFT_ROADMAP.md` LIFT-LOOP-P7, `__OutsideRepo/wave3/w3-restart-proof.txt` |
| B16 | **A field's auto-picker supports a declared filter/multi-select** (Move 9 A3) | `selectorRef` itself stays descoped (0 Java, 0 schema, never built) -- the real gap was that an FK field's `LOOKUP` widget had no way to constrain its candidate rows at all. `field.picker: { filter, multiSelect }` closes it: `filter` reuses `visibleWhen`'s `field == literal` / `field != literal` grammar and folds into the SAME `referenceSemantics.defaultFilter`/`defaultFilterExpression` mechanism the generated app's picker dialog already sends server-side as a `where` query param and the generated CRUD controller already enforces against real rows (`ConceptQueryFilterSupport`/`parseWhereClauses` -- proven, untouched code; this only adds a cleaner authoring surface on top). `multiSelect` sets the same `reference.multiple` flag the many-to-many bond picker already uses | Use `picker.filter`/`picker.multiSelect` directly on the reference field; the older `referenceSemantics.defaultFilter` colon/bare-equals grammar still works unchanged for existing declarations | A concrete need for a multi-clause (AND/OR) filter, which today's single-clause grammar (shared with `visibleWhen`/`query.where`) cannot express | AW-P2, `OPEN_GAPS_AND_ROADMAP.md`, Move 9 A3 |
| B17 | ~~Super-user key is issued, not supplied~~ **LIFTED, with a gate -- SEC-8, 2026-09-02** (REG-9 Q1) | The row's own risk (making it supplied invites weak operator-chosen keys) is what SEC-8 (`BOUNDARY_LIFT_PLAN_2026-09-02.md` work package 1.4) answers, not bypasses: `first boot -> key issued/supplied -> operator claims a named admin -> bootstrap key revoked -> normal auth`. `SuperUserBootstrapper` now supports three ways the bootstrap credential comes into being (unchanged issued default; `npdev.superuser.bootstrap-key-hash`, a deployment-generated key's SHA-256 hash, `npdev admin hash-key` -- no plaintext secret in a compose file; `npdev.superuser.bootstrap-key-raw`, refused at BOOT below a 24-char length floor, `B17:bootstrap_key_too_short:`) -- all three land ONE bootstrap credential, never a permanent identity. `POST /api/admin/superuser/claim` (`npdev admin create`), authenticated via the LIVE super-key path only (REG-22's marker, same as `ActuatorAdminGuardFilter`), issues a named administrator and revokes EXACTLY the credential that authenticated the call. A later attempt with the revoked bootstrap key gets `super_user_key_revoked`, distinct from `super_user_auth_failed` for a genuinely unknown key | Read the issued key from `SUPER_USER_KEY.txt` (default, unchanged), or configure a hash/raw bootstrap key for automated deployment; claim a named administrator promptly either way -- `npdev admin create --actor-id <name>` | — (closed) | REG-9 §2.1, SEC-8, `ledger/boundaries/B17.yml`, `SuperUserBootstrapper.java`, `SuperUserClaimController.java`, `CredentialRegistryService.java` |
| B18 | **Row-level write authorization's check-then-act window is closed under a real transaction manager** (Move 9 A2) | The revisit trigger fired: releasing authoring to arbitrary authors makes reassignable `access.write` rules the norm, not the exception. `DefaultConceptGateway.save()`/`delete()` now run their whole check-then-act body (read the row, evaluate `isRowWritable`, persist) inside a `TransactionRunner` (a small kernel port; the host wires a real `TransactionTemplate`, the same precedent `AggregateRuntime` established), using `ConceptStore.findByIdForUpdate` (a real `SELECT ... FOR UPDATE` on `JdbcBusinessConceptStore`) instead of a plain `findById` — a concurrent writer's own `findByIdForUpdate` against the same row now genuinely blocks until the first transaction commits, closing the window. `expectedRowVersion` was deliberately NOT defaulted on (no caller holds a version to compare against) | Nothing — no caller signature changed, `TransactionRunner.none()` (still every OTHER constructor's default) preserves the old degraded behavior only where no real transaction manager is wired (e.g. InMemory mode with no DataSource at all) | A degraded (no-transaction-manager) deployment still wants this guarantee — would need a different mechanism, since there is no transactional resource to lock in that mode | F8 `docs/archive/programme-history/DECISION_BRIEFS_2026-07.md`, LNCH13-F4 `docs/archive/programme-history/REG16_LNCH13_ROWLEVEL_AUTHZ_ADVERSARIAL_REVIEW.md`, `docs/reference/ROW_LEVEL_AUTHORIZATION.md`, Move 9 A2 |
| B19 | ~~`bandPickers` accepts the same `filter`/`multiSelect` shape a plain FK field's `picker` does, but `panel` is still required~~ **LIFTED, by construction -- EDIT-17, 2026-09-02** | `WorkbenchBandPickerAst`/`CompiledWorkbenchBandPicker` carry the SAME two properties `FieldPickerAst` declares -- one shape, two authoring surfaces. **What EDIT-17 (`BOUNDARY_LIFT_PLAN_2026-09-02.md` work package 1.1) closed**: `AutoPanelExpander.bandPickers()` (NPDevContract/dsl) now synthesizes a PROJECTION for a no-panel entry instead of skipping it -- targeting the band's own collection concept directly through the SAME generic reference-lookup endpoint (`/api/concepts/<table>`) an FK field's picker already uses, filtered server-side via the SAME `where=field:operator:value` mechanism B16 already proved end-to-end. `SqlIdentifierSupport.conceptEndpointBase` (new, shared with `BusinessUiEmitter.endpointBase` -- one definition, not two) resolves the bounded-context-aware route; a new filter-string parser mirrors (cannot depend on, DSL can't see the generator) `BusinessUiEmitter.parsePickerFilterExpression`'s exact grammar, validated against the band's own concept's real fields. `workbench-page.html.mustache`'s `openBandPicker` branches on `picker.panel` (unchanged) vs `picker.endpointBase` (new), normalizing both fetch shapes into the SAME existing checkbox/multi-add UI. `PanelValidation.validateBandPickers` now refuses ONLY a picker declaring neither panel nor filter/multiSelect (`B19:band_picker_unresolvable_concept:`, replacing the retired `band_picker_requires_panel`, which no longer fires for any reachable shape) | Declare `filter`/`multiSelect` on a `bandPickers` entry, with or without a `panel` -- the no-panel form needs no hand-authored Selection panel at all. See `NPDevSamples/dsl-conformance-max`'s `WidgetOrderNoPanelPickerWorkbench` for a real corpus example | — (closed) | AW-P2 (`7e1096e`), `docs/architecture/AGGREGATE_WORKBENCH_PLAN.md`, Move 9 A3, EDIT-17, `ledger/boundaries/B19.yml`, `NPDevContract/dsl/.../compiler/AutoPanelExpander.java`, `NPDevContract/dsl/.../compiled/SqlIdentifierSupport.java`, `NPDevGenerator/.../npdev-templates/workbench-page.html.mustache` |
| B20 | ~~No bounded contexts / multi-namespace models — one `namespace` = one flat vocabulary = one app~~ **LIFTED — S2 (contexts[]/imports[] mechanism), S3 (codemod + corpus migration), S8 Wave 4 (opt-in physical isolation), 2026-08-03/04, `docs/adr/ADR-0011-bounded-contexts.md`** | The old row's own measurement was correct as of 2026-07-28 and stayed the trigger: a real cross-domain-within-one-app need (the roadmap's framing: "a third person authoring a real app hits naming a second domain area in hour one") justified the compiler/schema cost named here. A model now declares `contexts: [{"name": "...", "$ref": "contexts/....model.json"}]` (D2: one context = one physical fragment file, composed via the existing pack/`$ref` machinery); cross-context references use `context::Concept` (D1, reusing pack-qualification's existing `::` syntax/mangling — zero new punctuation); an undeclared cross-context reference is a compile-time error, never a silent no-op (D3, explicit `imports[]`). **What did NOT change (D4, deliberately v1-scoped, still the default):** table names still derive from the bare concept name, ignoring any context qualifier — a context is a DSL/compiler-level boundary only, not a physical-storage or deployment one; two contexts both declaring the same concept name is a compile-time "duplicate concept" error, not a silent table collision. S8 Wave 4 added the named v2 escape from D4's own text: a context may opt IN, per-context, to `"physicallyIsolate": true`, which keeps its `::` qualifier in the derived table name instead of stripping it (default `false`; an app with no `contexts[]` at all, or a context that never sets the key, compiles to byte-identical table names as before — verified against WmsOffice's full realized schema, byte-identical DDL). Two real pre-existing gaps were found and fixed empirically while building this (not by inspection): REG-98 (the table-name-collision check hashed the qualified name, a different string than what the compiler actually derives, so it never caught its own named collision scenario) and a resolver-level malformed-`$ref` gate that hard-coded which keys a context entry may carry, predating `model.schema.json`'s own definition ever being consulted | Declare `contexts[]`/`imports[]` directly (see `docs/adr/ADR-0011-bounded-contexts.md` D1-D3 for the syntax, `NPDevSamples/dsl-conformance-max` for a real corpus witness with both isolated and non-isolated contexts). Still model a genuinely separate SERVICE (its own deployment, its own database instance) as its own app with its own `namespace` — contexts organize vocabulary WITHIN one app/one database, they do not replace "one model = one deployable app" (see the README's own "Honest limitations") | Blanket (always-on) table prefixing instead of the current opt-in-per-context default — would need a real migration story for every existing app's already-created tables, not just new ones, so stays a deliberate v2+ decision, not automatic | ADR-0011 (D1-D4, D8), REG-98, S8 Wave 4 (`__OutsideRepo/wave4/w4-wmsoffice-no-change.txt`), `NPDevSamples/dsl-conformance-max` |

| B28 | **Live model reload (LC-C3) — DO NOT SHIP; the app restarts to pick up a model change** (`MOVE10_AI_LOWCODE_PLAN.md` Part C.3, Move 12 P5) — **STALE UNTIL 2026-08-25 (W2.4a): the mechanism this row describes as never built was already built and REACHABLE** | LC-C3's own plan carries its HARD STOP verbatim: "if the derived graph cannot be swapped atomically, do not ship it. An app serving requests against a half-updated model is worse in every way than a 30-second restart." `CompiledModel` is a singleton bean with a large derived graph hanging off it (`CelInvariantEngine.fromCompiledModel`, `RuntimeConceptGatewaySemanticPolicies.fromCompiledModel`, `CompiledModelFlowDefinitionProvider`, sensitive-field registration, and more) — swapping the model without swapping every derived bean atomically produces an app whose validation rules and whose panels disagree, silently. **This row used to say no `ModelHolder` indirection existed "anywhere in NPDevRuntimeHost today — confirmed by grep, not assumed." That was true when written and became false without this row being updated: `ModelHolder.java` and `MetadataHotSwapController`'s `POST /api/admin/runtime/metadata-hotswap/model-reload` were both built**, giving only a PARTIAL swap (`ModelHolder.get()` consumers see the new model; every directly-injected `CompiledModel` singleton keeps the old one — exactly the split-brain state the HARD STOP above warns against) — **reachable by any caller holding SUPERUSER, with nothing stopping them from hitting it.** Found and fixed 2026-08-25 (W2.4a): the endpoint is now gated behind `npdev.runtime.hotswap.full-model-reload-enabled`, default `false`, checked AFTER the pre-existing SUPERUSER auth gate — disabled by default, returning a structured `FULL_MODEL_RELOAD_DISABLED` refusal. **LC-C2 (the metadata-only fast path, `RuntimeMetadataService`'s external-path override — REG-103, closed Move 12 P2.1) already removed most of the pain this would solve**: a metadata-only change (a label, a panel) can already reach a running app without a restart; only a genuinely schema/logic-shaped model change still needs one | Restart the app (~30 seconds) for a schema/logic-shaped model change; use LC-C2's metadata-only fast path (external `compiled-metadata.json`/`metadata/index.json` override) for everything LC-C2 already covers. The full-reload endpoint is opt-in only (`npdev.runtime.hotswap.full-model-reload-enabled=true`) — do not set it without accepting the split-brain risk above | A `ModelHolder`-style atomic-swap mechanism is designed and proven to swap the ENTIRE derived graph (not just `CompiledModel` itself) under concurrent load with zero requests observing a mixed model — migrating every direct `CompiledModel` injection to `ModelHolder.get()` (option (b) in the 2026-08-25 remediation plan) is a real engineering investment, not a flag flip. Until then, this boundary holds by an explicit gate, not by the endpoint simply not existing | `MOVE10_AI_LOWCODE_PLAN.md` Part C.3, REG-103, Move 12 P2.1/P5, `ledger/boundaries/B28.yml`, `MetadataHotSwapControllerStandaloneTest.java` |
| B29 | **SQL Server has no SUFFIX row cap -- `SqlDialect.rowLimit()` THROWS there** (storage/OPEN_ITEMS_PLAN.md 6, STOR-3's first engine gap) | `SELECT TOP n` is a PREFIX in T-SQL, and the only suffix form (`OFFSET 0 ROWS FETCH NEXT n ROWS ONLY`) requires an `ORDER BY` that an existence probe (`SELECT 1 FROM t WHERE c = ?`) does not have and should not need -- so there is no honest suffix answer on this engine. The plan asked for a decision between a prefix rewrite and declaring the capability absent, and said to **find the call sites first**. Measured: four, and none of them wants a suffix -- two existence probes in `PostgresPersistenceCapabilityAdapter` and two first-by-order reads in `JdbcEventStore`, all of which want "at most n rows". **Zero production sites call `rowLimit()` directly.** Throwing is the point rather than a gap: a plausible wrong answer in the least visible layer is the defect the whole `SqlDialect` seam exists to prevent, and this engine's own history is the proof -- every one of STOR-4, 5, 7, 9, 10 and 12 was a two-engine assumption that returned something plausible instead of refusing | Call `dialect.rowLimited(sql, n)`, which every engine answers, or `dialect.existsProbe(table, column)` for a dialect-BUILT probe. Both exist today and cover all four real sites | A real call site needs a bare SUFFIX on SQL Server and cannot be expressed as `rowLimited()` or a dialect-built statement. There is none today; if one appears the fix is a new dialect method that builds the whole statement, never a `rowLimit()` that returns something it cannot mean. **Deliberately NOT given a `StorageCapability` token**: the plan warns that "a thrown exception with no declared capability behind it is a sharp edge, not a boundary", and the zero-call-sites measurement is what answers that -- a token no caller consults would be dead code wearing a safety label | `ledger/boundaries/B29.yml`, `SqlServerDialect.rowLimit()`, storage/FULL_SUPPORT_PLAN.md W1.3 |
| B30 | **`plugin:java-source` is OS-process-isolated with a measured, enforced memory/CPU ceiling; `plugin:java-controller` still runs in-process, admission-checked NOT sandboxed** (SEC-3 Model A, 2026-08-27; Model B for java-source -- SEC-5/SEC-6, 2026-09-01, defaults ON -- SEC-7, 2026-09-02) | Author-supplied compiled Java must run somewhere. Static-analysis admission (Model A, unchanged) sits at BOTH ends of the pipeline over ONE shared constant-pool denylist (`TrustedSourceBytecodeInspector`, NPDevKernel): **generation time** -- `PluginJavaSourcePolicy` refuses emission of plugin source referencing the escapes (filesystem/network IO, Runtime/Process/ProcessBuilder, reflection, Class/ClassLoader/ServiceLoader, Thread/Timer, executor/async detachment, javax.script., sun./jdk. internals, System.exit/getenv/getProperty*); **boot time** -- `PluginBytecodeBootGate` inspects every plugin-owned COMPILED class file (plus its `$`-siblings) and refuses app start with `B30:plugin_bytecode_violation:` via `BoundaryBootException`. **For `plugin:java-source` specifically, Model B is now shipped on top of Model A**: every invocation dispatches through `PluginIpcChildProcessPool` -- a real separate OS process per worker, wired into production dispatch (SEC-5), with its own measured timeout floor (SEC-6: 5000ms, against a real measured 223-393ms cold start) -- and `PluginProcessResourceLimiter.forCurrentOs()` attaches a real OS-level ceiling per worker (Windows Job Object hard memory+CPU caps, or Linux `systemd-run --scope`/`MemoryMax=`), ON by default since SEC-7 at `memory-limit-mb=512` / `cpu-rate-percent=50` -- chosen from a real measurement against a live `plugin:java-source` mount (Guava-hashing SignWithLibrary flow, 8 real HTTP calls + a CRUD create; peaked ~91MB per worker under the 512MB ceiling, left generous rather than tightened to the observed peak). A host with no real OS-level mechanism available degrades to logging a loud WARNING at boot (pool workers start synchronously in the app's own startup), never silently. **The residue, narrowed but not gone**: `plugin:java-controller` is UNCHANGED by any of this -- still in-process, memory/CPU unbounded, admission-checked only. **Wave 4 (package 4.4, REG-204, 2026-09-03) answers the open design question this row used to carry**: `plugin:java-controller` is declared a TRUSTED tier BY DESIGN (Analisis 2's trusted/declarative/isolated ladder) rather than pursuing servlet-level interception -- a controller mount is a raw Spring `@RestController` wired by `DispatcherServlet`, with no `CapabilityCall` interception point a process boundary could sit behind (SEC-5's own reason for excluding it from the IPC pool, unchanged and still correct). `RuntimeApiEmitter.emitJavaControllerMounts` now PRINTS this at generation time -- `npdev generate app: WARNING (B30) -- plugin:java-controller '<capability>' ... TRUSTED tier by design ...` -- once per mounted controller, naming its capability, so an author sees it for every app that actually mounts one rather than needing to already know to read this doc row. Servlet-level interception (routing a controller mount through the same process boundary `plugin:java-source` gets) remains a real, recorded option, deliberately not built now; and even for `plugin:java-source`, static analysis is still the only admission gate -- a proxy class elsewhere on the app classpath performing a forbidden capability on a plugin's behalf is outside a constant-pool scan's view, and a plugin busy-looping inside its own bounded process without exceeding the memory/CPU ceiling is bounded but not stopped early | For `plugin:java-source`, the OS-level ceiling is on by default; retune `npdev.runtime.plugin-ipc-pool.memory-limit-mb`/`.cpu-rate-percent` if a real plugin's workload needs more headroom. For `plugin:java-controller`, only mount plugins you trust -- it is application code with full privileges (the plugin:java-* docs say exactly this, and the generator now says it too, at generation time, for every mount) | Real servlet-level interception for `plugin:java-controller` (routing it through the same process boundary `plugin:java-source` gets), ONLY if third-party plugin distribution becomes a product goal -- the trusted-tier-by-design decision (package 4.4) is otherwise permanent, not a placeholder | SEC-3, SEC-5, SEC-6, SEC-7, REG-204, `ledger/boundaries/B30.yml`, `NPDevKernel/kernel/.../security/TrustedSourceBytecodeInspector.java`, `NPDevGenerator/.../emitters/PluginJavaSourcePolicy.java`, `NPDevGenerator/.../emitters/RuntimeApiEmitter.java#emitJavaControllerMounts`, `NPDevRuntimeHost/.../config/PluginBytecodeBootGate.java`, `NPDevRuntimeHost/.../npdev/service/pluginipc/PluginIpcChildProcessPool.java`, `PluginProcessResourceLimiter.java`, `docs/architecture/PLUGIN_PROCESS_ISOLATION_DESIGN.md` |
| B27 | ~~`query.groupBy`/`query.aggregates` (Move 10 B1) is single-concept only — no cross-concept join~~ **LIFTED (groupBy only) — S4, 2026-08-03; widened to 3 hops — S8 W1.1, 2026-08-04** (roadmap B27, ADR-0011 D1, REG-105) | B20 (S2, ADR-0011) was sequenced first specifically to make this liftable without ambiguity: `context::Concept` already claims `::`, leaving `.` free for a join hop, so `groupBy: ["lote.produtoId"]` (same-context) and `groupBy: ["inventory::lote.produtoId"]` (cross-context) both parse unambiguously (`GroupByJoinGrammar`, `NPDevContract/dsl/.../query/`). `lote` must be a declared `reference` field on the query's own concept; the join resolves to `(context, concept, field)` and reaches the SAME `requireColumn` choke point a bare field already used, exactly the extension point `b20-design.md` §3 item 8 named. Scoped narrowly at S4 ship time: **one hop only** (`a.b.c` was a named compile error) and **`groupBy` only** — `where`/`aggregates` still name a field on the query's own base concept, unchanged. The `access.read` hard stop (already refusing `groupBy`/`aggregates` on a restricted concept) widens to the WHOLE join path, both at compile time (`PackValidation#validateAggregateQuery`) and as a runtime backstop (`DefaultConceptGateway#aggregate`, via a new `ConceptGatewaySemanticPolicy#resolveReferenceTarget` extension point) — a join makes the leak strictly worse, not something a join gets to bypass. JDBC emits a real parameterized `INNER JOIN` (captured SQL, not inferred from row counts); the in-memory adapter pre-materializes the same join in Java before handing rows to the unmodified `ConceptAggregateEngine`, excluding a record whose reference is null or dangling — the SAME rows both engines exclude, proven by a parity test running the identical query against both. **S8 W1.1 (2026-08-04, roadmap deferred item #1) widened the one-hop cap to `GroupByJoinGrammar.MAX_JOIN_HOPS` = 3**: the join now walks a CHAIN of reference fields (`referenceFields: List<String>`, not a single field), `access.read` is re-checked at EVERY hop crossed (not just the one/first), and a path longer than 3 hops (`a.b.c.d.e`) is still a named compile error, never silently truncated. `LocalArmazenagemLote → Lote → Produto` (the WmsOffice case that named this boundary) is now FULLY expressible as a 2-hop `groupBy` — proven by a 2-hop cross-engine parity test (`ConceptAggregateGroupByJoinPushDownTest#groupByTwoHopJoinAgreesAcrossEngines`) mirroring the 1-hop one exactly, plus a `dsl-conformance-max` corpus witness (shipping.json's `DeliveryAttemptsByInvoiceStatus`, 2 hops through `shipment.invoice.status`) | For a same-context or cross-context join of up to 3 hops, declare it directly: `groupBy: ["lote.produtoId"]` (1 hop) or `groupBy: ["a.b.c.d"]` (3 hops, at the cap); an optional `context::` prefix names the FINAL joined concept. For a chain LONGER than 3 hops, the capability-backed data-source workaround this row originally named still applies — `listConcepts`+`mapList`+a Java capability join — since a path beyond the cap is refused, not silently truncated | A real app needs a join deeper than 3 hops badly enough to justify raising `GroupByJoinGrammar.MAX_JOIN_HOPS` again, or `where`/`aggregates` need the same join capability `groupBy` has | REG-105, ADR-0011 (D1, D3), `NPDevContract/dsl/.../query/GroupByJoinGrammar.java`, `NPDevContract/dsl/.../validation/PackValidation.java#validateAggregateQuery`, `NPDevRuntimeHost/.../db/JdbcBusinessConceptStore.java#aggregate`, `NPDevKernel/kernel/.../inproc/InMemoryConceptStore.java#aggregate`, `NPDevSamples/dsl-conformance-max` (shipping.json: `DeliveryAttemptsByCarrier` same-context 1-hop, `ShipmentsByInvoiceStatus` cross-context 1-hop, `DeliveryAttemptsByInvoiceStatus` 2-hop), the generated DSL reference (`scripts/docs/generate_dsl_reference.py`; the rendered `DSL_REFERENCE.md` is no longer committed — run the generator if you want it) |

## 3. Reading these correctly

Three of these boundaries exist because **the safe answer is to refuse rather than guess**: B1 (rename),
B4 (concurrency), B5 (schema-ahead). That is the schema engine's governing instinct — when intent cannot
be recovered from shape, stop and ask a human. Several others (B3, B12, B13) exist because **the
conservative half of a feature is worth shipping without the dangerous half**.

If you are about to remove one of these, the question to answer in writing is: *what new information do
we have that the original decision lacked?* If the answer is "none, it just feels incomplete" — leave it.
