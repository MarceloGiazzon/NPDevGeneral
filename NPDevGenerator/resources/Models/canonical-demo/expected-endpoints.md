# Expected Endpoints

## Canonical runtime surfaces

- `GET /api/flows`
- `GET /api/admin/model/export`
- `GET /api/admin/model/metadata`
- `GET /api/audit`

## Flow/runtime expectation

The canonical demo should expose `GET /api/flows` and at least one `/api/` admin/runtime evidence surface suitable for regression checks.
