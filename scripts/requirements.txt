# Python dependencies for scripts/ that are not stdlib-only.
#
# scripts/quality/check-register-consistency.py (Rule T3, docs/REMEDIATION_PLAN.md R-D2) and
# scripts/quality/generate_open_items.py both parse ledger/items/*.yml. Every other script under
# scripts/ is stdlib-only. Installed by .github/workflows/ai-knowledge-gate.yml before running
# either script -- see ledger/README.md.
PyYAML>=6.0.1

# scripts/quality/check-config-schema.py validates every corpus config.json against
# config.schema.json and deliberately REFUSES to skip if this import fails ("a validation gate
# that silently does nothing when its validator is missing is the exact shape of defect it was
# written to catch"). NPDevCli/tests/test_engines.py also imports it directly (its own comment
# already assumed this was declared -- it wasn't, which is how this entry went missing until CI
# actually ran this gate against docs-decoupling-2026-08-11 on 2026-08-11 and both failed with
# ModuleNotFoundError). NOT a dependency of the shipped CLI itself -- npdev_jsonschema.py is a
# deliberate stdlib-only validator for that (see its own docstring: the Manager ships a private
# Python with no third-party packages), so this entry is for repo-development/CI use only.
jsonschema>=4.0
