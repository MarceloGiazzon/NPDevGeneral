{
  "version": "1.1",
  "betaRelease": {
    "includedSurfaces": [
      "contract",
      "editor",
      "generator",
      "kernel",
      "runtimehost",
      "sample-matrix",
      "ai-beta",
      "frontend-audit-policy",
      "runtime-surface-evidence",
      "npdev-cli-runtime"
    ],
    "excludedSurfaces": [],
    "betaScopeTransitions": [
      {
        "id": "npdev-cli-runtime",
        "status": "beta-covered",
        "owner": "NPDevGenerator",
        "reason": "CLI runtime semantic beta coverage is restored; closure evidence is retained outside excludedSurfaces so current inclusion and exclusion lists remain semantically clean.",
        "reentryCondition": "No remaining CLI/runtime semantic exclusions.",
        "disabledTests": [],
        "closureItems": [
          {
            "priority": 1,
            "test": "com.npdev.cli.NPDevCliMainTest#executeAndTraceCommandsWorkWithSharedStoreDir",
            "status": "re-enabled-this-cycle",
            "owner": "NPDevGenerator",
            "implementationNote": "The CLI regression now executes CreateUser against a shared --store-dir, persists cli-state.json, and resolves the saved trace by the returned executionId."
          },
          {
            "priority": 2,
            "test": "com.npdev.cli.NPDevCliMainTest#waitingPublishAndResumeLifecycleWorksInCli",
            "status": "re-enabled-this-cycle",
            "owner": "NPDevGenerator",
            "implementationNote": "The CLI AwaitDemo regression now publishes SomethingHappened with the execution correlation id and asserts the waiting execution resumes and disappears from the waiting list."
          },
          {
            "priority": 3,
            "test": "com.npdev.cli.NPDevCliMainTest#idempotencyHitReturnsCachedResultMetadata",
            "status": "re-enabled-this-cycle",
            "owner": "NPDevGenerator",
            "implementationNote": "The CLI idempotency regression now executes the same request twice against a shared store, asserts the cached output is reused, and verifies the trace plus persisted idempotency record expose the HIT metadata."
          },
          {
            "priority": 4,
            "test": "com.npdev.cli.NPDevCliMainTest#circuitBreakerCanBeExercisedFromCliSimulation",
            "status": "re-enabled-this-cycle",
            "owner": "NPDevGenerator",
            "implementationNote": "The CLI circuit-breaker regression now drives five transient save failures, asserts the sixth execution is short-circuited with CAPABILITY_CIRCUIT_OPEN, and verifies OPEN state in both the trace and persisted circuit store."
          },
          {
            "priority": 5,
            "test": "com.npdev.cli.runtime.CliRuntimeFactoryBulkheadTest#inprocBulkheadRejectsWhenConcurrencyLimitIsExceeded",
            "status": "re-enabled-this-cycle",
            "owner": "NPDevGenerator",
            "implementationNote": "The CLI runtime bulkhead regression now launches a synchronized burst of concurrent executions against one runtime instance and asserts CAPABILITY_BULKHEAD_FULL with REJECTED bulkhead trace metadata."
          }
        ]
      }
    ]
  }
}
