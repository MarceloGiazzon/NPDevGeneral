{
  "$comment": [
    "BASELINE for check-out-of-tree-generation.ps1, established 2026-08-10 by the first real",
    "out-of-tree generation this repo has ever run (npdev-canary, 807 files scanned, 15 hits).",
    "",
    "The gate is a RATCHET, not a clean bill of health. Every entry below is a path pattern whose",
    "absolute-path leak was ALREADY PRESENT when the check was written. A NEW leak anywhere else",
    "fails immediately -- which is the whole point, since eight defects of exactly this family",
    "reached a third party while every gate stayed green.",
    "",
    "Two categories, and the difference is the entire honesty of this file:",
    "  accepted     -- correct as-is; will never be 'fixed'. Provenance records and comments that",
    "                  DISCUSS the defect are not instances of it.",
    "  known-defect -- a real leak, filed, still present. These are NOT excused. Each names its",
    "                  ledger item. When one is fixed, DELETE its entry so the ratchet tightens.",
    "",
    "Do not add an entry without a reason. An allowlist that states something untrue is worse than",
    "no allowlist (the lesson STOR-13's own allowlist taught this repo).",
    "",
    "2026-08-10, same day: THE known-defect LIST IS NOW EMPTY. All six of PORT-1's leaks are fixed --",
    "application-wmsoffice.yml and AuxScreen.js earlier in the day, and in this change the four that",
    "remained: application-npdev-db.properties + _ops/resolved-db-plan.json (one fix -- the data root",
    "is app-relative now, so spring.datasource.url resolves against the app rather than against the",
    "authoring machine), model-source-manifest.json and schema-realization-manifest.json (paths",
    "recorded relative to the authoring directory, provenance kept in Reports/ where it belongs).",
    "",
    "The ratchet has not been loosened to get here: every `accepted` entry below is still an",
    "accepted entry, and a NEW leak anywhere still fails on the first run. What is gone is the",
    "excuse list. If a known-defect entry ever reappears, it is a regression, not a baseline."
  ],
  "entries": [
    {
      "pattern": "App/gradle.properties",
      "category": "accepted",
      "reason": "The match is INSIDE a comment explaining why a hardcoded D:/WorkSpace build-output path must not be used. Prose about the defect is not the defect."
    },
    {
      "pattern": "App/_ops/Create-Environment.ps1",
      "category": "accepted",
      "reason": "Same shape: a comment recording that this line USED to read the author's drive letter and no longer does. Deleting the comment to satisfy a grep would destroy the record of the fix."
    },
    {
      "pattern": "Reports/generation-run.json",
      "category": "accepted",
      "reason": "Provenance. Records which workspace produced this run and is already declared non-reproducible by check-deterministic-generation.ps1. Not shipped inside the app."
    },
    {
      "pattern": "App/src/main/resources/npdev-build-info.properties",
      "category": "accepted",
      "reason": "Provenance, same declared exclusion as generation-run.json. Records the model's source path at build time; nothing resolves it at runtime."
    }
  ]
}
